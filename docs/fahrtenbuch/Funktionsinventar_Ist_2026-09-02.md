# Funktionsinventar IST-Zustand — ParlamentPlattform (Django 5, v0.32.0)

Quelle: ausschließlich der Code unter `/home/claude/ddoe/parlamentplattform-phase0` (Templates, Views, Models, URLs, CSS/JS, YAML, Management-Commands). CHANGELOG/README dienten nur als Wegweiser. Belege sind als `Datei:Zeile` angegeben (Pfade relativ zum Projektstamm). Stand der Erhebung: 2026-09-02. Testbasis: `pytest --collect-only` → 265 Tests (260 Testfunktionen, `tests/test_policy.py` ist 6-fach parametrisiert); `manage.py check` ohne Befund.

---

# 1. Architektur-Überblick

## 1.1 Apps

| App | Zweck | Models | Views | Tests (`def test_`) |
|---|---|---|---|---|
| `verfahren` | Kern des Verfahrens: Anträge, Fassungen, Unterstützungen, pseudonyme Stimmen + Stimmregister, Kategorienbaum/Abos, Favoriten, WeicherFilter-Profile, Mandats-Kandidaturen (Bewerbung/Zustimmung), Umsetzungsregister, Audit-Hash-Kette. Trägt `base.html`, Startseite, Parlament, Antragsseite, Einbringen, Zukunftswerkstatt, Partner. | 15 (+3 TextChoices) | 25 (8 in `views.py`, 17 in `views_aktionen.py`) | 84 (9 Dateien) |
| `mitglieder` | Eigenes User-Modell `Mitglied` (AbstractUser), Double-Opt-in-Registrierung, passwortloser Magic-Link-Login, Gemeindeverzeichnis, Mitgliedschaftsseite, Einführung (3 Schritte), Beitragsseite mit EPC-QR, Beitragsabgleich (GoCardless-API oder camt.053/CSV-Upload), Mitglieder-Verwaltung (Admins). | 5 (`Mitglied`, `Gemeinde`, `Bankkopplung`, `Beitragseingang`, `EinmalToken` in `auth_flows.py`) | 18 | 39 (4 Dateien) |
| `uebersicht` | Öffentliche „Plattform in Zahlen" mit servergerenderten SVG-Diagrammen; datensparsame Besuchszählung per Middleware (Tages-Summen, Einweg-Tageskennung). | 3 | 1 | 5 |
| `anstoss` | Feedback-Widget auf jeder Seite („Anstoß"), Speicherung in eigener DB, Verwaltung mit Statusfilter und CSV/JSON-Export. | 1 | 5 | 10 |
| `mandatare` | Öffentliche Mandatar-Seiten (Foto in DB, Aufgaben mit Fristen, verknüpfte Anträge) und deren Pflege durch die Verwaltung (Stufe M1). | 2 | 5 | 8 |
| `gremien` | Gremien-Werkstatt Ring 0a: Rollen auf Zeit, Entwurfsfenster des Expertenrats (Gruppe 1), interne Einreich-Abstimmung, Prüfung durch Gruppe 2, Koordinationsrat (Austauschanträge), Unterstützer-Votum (Entwurfsschleife § 5 Abs 12), Rollen-Verwaltung. | 7 | 12 | 28 (2 Dateien) |
| `ki` | Anbieterneutraler Modell-Steckplatz (Mistral via urllib, Attrappe), append-only Lauf-Archiv `KILauf`, Monats-Tokenbudget. Keine eigenen URLs/Views. | 1 | 0 | 9 |
| `parameter` | Öffentliches Parameterregister (5 Erstbestands-Einträge), JSON-Export, Verwaltung mit Pflicht-Grund + Audit. | 1 | 4 | 8 |
| `plattform_core` | Framework-freier Kern: Phasenautomat, Policy, Auszählung (Sach- und Personenwahl), Stimmberechtigung, Hash-Kette, Ähnlichkeit, Klassifikation, Fächer-Layout, WeicherFilter-Regel, SVG-Diagramme, Beitragsabgleich, Kontoauszug-Leser. | — | — | 20 (im Paket) + 49 in `tests/` (54 gesammelt) |
| `verify` | `verify/nachrechnen.py`: unabhängige Zweitimplementierung der Sach-Auszählung (nur Stdlib) über den JSON-Export. | — | — | — |

Summe: 35 Models, 70 View-Funktionen, 73 URL-Einträge, 265 gesammelte Tests.

## 1.2 `config/settings.py`

**Umgebungsvariablen** (alle mit Default und Bedeutung):

| Variable | Default | Bedeutung | Beleg |
|---|---|---|---|
| `DDOE_SECRET_KEY` | `"nur-fuer-entwicklung-niemals-produktiv"` | Django `SECRET_KEY` (auch Salz der Besucher-Tageskennung, `uebersicht/models.py:69`) | `settings.py:13` |
| `DDOE_DEBUG` | `"1"` (= DEBUG an) | Debug-Modus; `"0"` schaltet Security-Block scharf | `:14` |
| `DDOE_ALLOWED_HOSTS` | `localhost,127.0.0.1` (+`testserver` bei DEBUG) | `ALLOWED_HOSTS`, kommagetrennt | `:15-17` |
| `POSTGRES_HOST` | — (leer ⇒ SQLite `entwicklung.sqlite3`) | Schaltet PostgreSQL ein | `:66-83` |
| `POSTGRES_DB` / `POSTGRES_USER` / `POSTGRES_PASSWORD` / `POSTGRES_PORT` | `plattform` / `plattform` / `""` / `5432` | DB-Zugang | `:70-74` |
| `DDOE_SMTP_HOST` | — (leer ⇒ Konsolen-Mailbackend) | SMTP-Server; setzt `EMAIL_USE_TLS=True` | `:111-122` |
| `DDOE_SMTP_PORT` / `DDOE_SMTP_USER` / `DDOE_SMTP_PASSWORT` | `587` / `""` / `""` | SMTP-Zugang | `:114-116` |
| `DDOE_SMTP_TIMEOUT` | `20` (s) | `EMAIL_TIMEOUT` | `:120` |
| `DDOE_MAIL_ABSENDER` | `ParlamentPlattform <plattform@ddoe.at>` | `DEFAULT_FROM_EMAIL` | `:123` |
| `DDOE_UEBERGANGSREGEL` | `"1"` | § 4 Abs 4 lit d: Anwartschaftsfristen entfallen | `:129` |
| `DDOE_FIX_ADMIN` | `didide@ddoe.at` | E-Mail des unantastbaren Erst-Admins | `:134` |
| `DDOE_BANK_SECRET_ID` / `DDOE_BANK_SECRET_KEY` | `""` | GoCardless Bank Account Data | `:139-140` |
| `DDOE_BASIS_URL` | `https://parlament.ddoe.at` | Rückkehr-Link Bankkopplung, Links in Erinnerungsmails | `:141` |
| `DDOE_CSRF_ORIGINS` | `""` | `CSRF_TRUSTED_ORIGINS`, kommagetrennt | `:158` |
| `DDOE_STATIK` | — (`whitenoise` aktiviert WhiteNoise-Middleware + `CompressedStaticFilesStorage`) | Statik in Produktion | `:162-167` |
| `DDOE_KI_ANBIETER` | `mistral` (`attrappe` = Test-Anbieter) | Modell-Steckplatz | `:171` |
| `DDOE_KI_SCHLUESSEL` | `""` (leer ⇒ Steckplatz aus) | API-Schlüssel | `:172` |
| `DDOE_KI_MODELL` | `mistral-small-latest` | Modellname | `:173` |
| `DDOE_KI_MONATSTOKENS` | `1000000` | Rückfall-Budget vor Parameterregister | `:174` |
| `DDOE_BOT_MINDESTZEIT` | `5` (s) — nur via `getattr(settings, …)` | Mindestzeit Formular-Absenden (Botschutz) | `mitglieder/botschutz.py:36` |

**Middleware** (`settings.py:36-46`, in Reihenfolge): `SecurityMiddleware` → (bei `DDOE_STATIK=whitenoise` an Position 1: `WhiteNoiseMiddleware`) → `SessionMiddleware` → `LocaleMiddleware` → `CommonMiddleware` → `CsrfViewMiddleware` → `AuthenticationMiddleware` → `MessageMiddleware` → `XFrameOptionsMiddleware` → `uebersicht.middleware.Besuchszaehlung`.

**Installierte Apps** (`:19-34`): admin, auth, contenttypes, sessions, messages, staticfiles, `mitglieder`, `verfahren`, `uebersicht`, `anstoss`, `mandatare`, `gremien`, `ki`, `parameter`. (`django.contrib.admin` ist installiert, aber **nicht geroutet** — `config/urls.py:3-4`.)

**i18n** (`:93-103`): `LANGUAGE_CODE="de-at"`, `TIME_ZONE="Europe/Vienna"`, `USE_I18N`, `USE_TZ`, `LANGUAGES=[("de","Deutsch"),("en","English")]`, `LOCALE_PATHS=[BASE_DIR/"locale"]`. Umschalter über `django.conf.urls.i18n` (`config/urls.py:17`).

**Static** (`:105-106`): `STATIC_URL="static/"`, `STATIC_ROOT=BASE_DIR/"staticfiles"`; App-Static nur `verfahren/static/verfahren/js/{htmx,alpine}.min.js`.

**Auth/Session/CSRF/Security**: `AUTH_USER_MODEL="mitglieder.Mitglied"` (`:85`); Passwort-Validatoren mit min. 12 Zeichen (`:87-91`) — obwohl Konten ohne nutzbares Passwort angelegt werden (`mitglieder/views.py:133`); `LOGIN_URL="/anmelden/"` (`:125`). Bei `DEBUG=False` (`:144-154`): `SECURE_SSL_REDIRECT`, `SESSION_COOKIE_SECURE`, `CSRF_COOKIE_SECURE`, HSTS 30 Tage inkl. Subdomains, `SECURE_REFERRER_POLICY="same-origin"`, `SECURE_PROXY_SSL_HEADER=("HTTP_X_FORWARDED_PROTO","https")`. Keine CSP, kein manuelles Session-Alter (Django-Default 2 Wochen). Cache: Django-Default (LocMem, prozesslokal) — davon hängt die Drossel in `botschutz.py:94-105` ab.

## 1.3 Datenmodell

### verfahren (`verfahren/models.py`)
- **Verfahrensordnung** (`:41-60`): `policy_id` Slug, `version` PosInt, `regeln` JSON, `beschlossen_am` DT?, `aktiv` Bool; unique (`policy_id`,`version`); `als_policy()` → `Policy.aus_dict`.
- **Ebene** TextChoices (`:63-69`): bund/land/bezirk/gemeinde.
- **Kategorie** (`:72-150`): `slug` unique, `name`, `eltern` FK self (PROTECT, `kinder`), `beschreibung`, `eurovoc` Char, `schlagworte` JSON-Liste, `reihenfolge`, `aktiv`. Properties `pfad` (" › "-Kette), `pfad_kurz` (letzte 3 Ebenen), `tiefe`; Methoden `vorfahren()`, `nachfahren_ids()`.
- **Antragsart** TextChoices (`:153-159`): sache/mandat.
- **Antrag** (`:162-351`)
  - Felder: `titel` (200), `art` (sache/mandat), `eingebracht_von` FK Mitglied (PROTECT, `antraege`), `eingebracht_am`, `phase` (Werte aus `Phase`, Default unterstuetzung), `phase_beginn`, `policy_snapshot` JSON (eingefrorene Policy), `stimmberechtigte_anzahl` (bei Abstimmungsbeginn festgestellt, danach unverändert), `zurueckweisung_begruendung` (**nie geschrieben — kein Code-Pfad**), `ebene`, `gebiet`, `hervorgehoben` Bool + `hervorhebung_begruendung` (**nur `demo_seed` schreibt sie, kein UI**), `kategorien` M2M (`antraege`).
  - `policy()`, `aktueller_text()` (höchste Fassung), `stimme_zulaessig()`, `vollzugsstand()` (jüngster Vollzugseintrag).
  - **`fortschreiben(jetzt)`** (`:237-316`): lazy-idempotenter Phasenautomat. In BERATUNG wird zuerst `gremien.Entwurf.fortschreiben()` befragt (kann selbst die Endabstimmung öffnen); dann `naechster_uebergang(...)`; ein Übergang BERATUNG→ABSTIMMUNG wird zurückgehalten, solange `entwurf.haelt_beratung_offen()`; beim Wechsel nach ABSTIMMUNG wird `stimmberechtigte_anzahl` per `stimmberechtigte_zaehlen` festgestellt (Gegenstand PERSONENWAHL bei `mandat`, sonst SACHFRAGE; `max(1, …)`); Audit `phasenwechsel` mit `wirksam_ab` und Grund.
  - `auszaehlen()`: Sachantrag → `tally.auszaehlen`; Mandat → `kandidatur_auszaehlen()` → `personenwahl_auszaehlen` (wählbare = nicht zurückgezogene Bewerbungen in Einreichreihenfolge).
- **AntragsFassung** (`:354-370`): `antrag` FK, `nummer`, `wortlaut`, `begruendung`, `erstellt_am`; unique (antrag, nummer).
- **Unterstuetzung** (`:373-384`): antrag, mitglied, `erklaert_am`; unique je Mitglied.
- **Stimmabgabe** (`:387-401`): antrag, `pseudonym` UUID, `stimme` (ja/nein/enthaltung), `abgegeben_am`; unique (antrag, pseudonym). **Ohne Personenbezug.**
- **StimmRegister** (`:404-426`): antrag, mitglied, `pseudonym`, `pruefcode` (16 Hex, `secrets.token_hex(8)`); unique (antrag, mitglied). Verbindung Mensch↔Stimme; nicht im Admin registriert.
- **KategorieAbo** (`:429-445`): kategorie, mitglied, erstellt_am; unique.
- **Favorit** (`:448-463`): antrag, mitglied; unique. Rein persönlich.
- **Vollzugsstatus** (`:466-473`): offen / in_umsetzung / blockiert / umgesetzt / zurueckgestellt.
- **Vollzugseintrag** (`:476-501`): antrag (`vollzug`), status, `vermerk` (≤2000), erstellt_am, `durch` FK; Ordering neueste zuerst; append-only. Funktion `vollzug_fortschreiben()` (`:504-515`): nur Phase ANGENOMMEN, Audit `vollzug`.
- **AuditEintrag** (`:518-540`): `lfd` BigAuto PK, `zeit`, `ereignis` JSON, `hash` (64). `anhaengen(ereignis)` verkettet SHA-256 mit Vorgänger (GENESIS `"0"*64`).
- **FilterProfil** (`:580-606`): mitglied (`filterprofile`), `name` (40), `regler` JSON, `aktiv`, `geaendert_am`; unique (mitglied, name); `HOECHSTZAHL = 5`.
- **Bewerbung** (`:609-634`): antrag (`bewerbungen`), mitglied (PROTECT), `vorstellung` (≤2000, öffentlich), erstellt_am, `zurueckgezogen`; unique je Mitglied.
- **BewerbungsZustimmung** (`:637-656`): bewerbung (`zustimmungen`), `pseudonym` UUID, abgegeben_am; unique (bewerbung, pseudonym).
- **Kommentar** (`:745-759`): antrag (`kommentare`), mitglied (PROTECT), `text` (≤4000), erstellt_am; Ordering chronologisch.
- Fachoperationen (einzige Schreibwege): `antrag_einbringen()` (`:546-577`, friert Policy ein, Fassung 1, Audit `antrag_eingebracht`), `bewerbung_einreichen()` (`:663-682`, bis Abstimmungsbeginn, Rückzug wird aufgehoben, Audit), `bewerbung_zustimmen()` (`:685-713`, Umschalter, geheim über Stimmregister, Audit ohne Bewerbungs-ID), `stimme_abgeben()` (`:720-742`, update_or_create, Audit `stimme`/`stimme_geaendert` ohne Wert), `kategorien_zuordnen()` (`:762-785`, Schlagwort-Klassifikation, Audit `kategorien_zugeordnet` Methode `schlagworte-v1`).

### mitglieder (`mitglieder/models.py`, `auth_flows.py`)
- **Bundesland** TextChoices (9 Länder, `:17-26`). **Identitaetsstufe** (`:29-33`): ungeprueft / geprueft („Einladungscode nach Identitätsfeststellung") / praesenz (§ 13 Abs 2) / eid (§ 2 Abs 4). **Mitgliedsstatus** (`:36-44`): aktiv / pausiert / ausgeschlossen.
- **Mitglied** (AbstractUser, `:47-142`)
  - Eigene Felder: `beitritt` Date? (Beginn der Anwartschaft), `identitaetsstufe`, `pseudonym_oeffentlich` (50), `gemeinde` Char, `bundesland` Choice, `wohnsitz` FK Gemeinde (SET_NULL), `status`, `status_grund`, `beitrag_zuletzt_am` Date?, `ist_admin` Bool. Kein Geburtsjahr, kein Profilbild, keine Telefonnummer.
  - `ist_stimmberechtigt(gegenstand, stichtag, uebergang)`: Beitritt gesetzt ∧ Identität ≠ ungeprüft ∧ Status aktiv ∧ `plattform_core.stimmberechtigt`.
  - Properties: `anzeigename` (Pseudonym → Klarname → username), `ist_fixer_admin` (E-Mail == `DDOE_FIX_ADMIN`), `hat_adminrechte` (aktiv ∧ (ist_admin ∨ fixer Admin)), `darf_mitwirken` (aktiv ∧ status aktiv), `hat_gremienrolle` (aktive `Rolle` in irgendeinem Gremium — auch Integritätsrat).
- `stimmberechtigte_zaehlen(gegenstand, stichtag, uebergang)` (`:145-157`): zählt iterativ (Python-Schleife) alle aktiven, geprüften Mitglieder.
- **Gemeinde** (`:160-208`): `kennziffer` (5, unique), `name`, `bezirk`, `bundesland`; `anzeige` „Name (Bezirk)"; `name_normalisieren` („Sankt"→„St."); `finden(eingabe)` → (treffer, kandidaten). Quelle `daten/gemeinden.csv` (2092 Zeilen Daten, Statistik Austria 2026).
- **Bankkopplung** (`:211-260`): `requisition_id`, `account_id`, `institution_id`, `gekoppelt_am`, `consent_bis`, `zuletzt_abgerufen`, `abruf_tag`, `abrufe_heute`, `aktiv`; `ABRUFE_PRO_TAG = 4`; `aktuelle()`, `abruf_erlaubt()`, `abruf_vermerken()`.
- **Beitragseingang** (`:263-281`): mitglied (`beitraege`), `betrag` Decimal(9,2), `gebucht_am`, `umsatz_id` unique, `namens_hinweis` Bool, `erfasst_am`. `beitrag_verbuchen()` (`:284-338`): idempotent über Umsatz-ID; hebt `beitrag_zuletzt_am`, pausiert→aktiv, ungeprüft→geprüft; Audit `beitrag/eingang_verbucht` (ohne Betrag); Bestätigungsmail (Link hart `https://parlament.ddoe.at/beitrag/`, `:332`).
- **EinmalToken** (`auth_flows.py:32-79`): mitglied, `zweck` (bestaetigung/login), `token_hash` SHA-256 unique, erstellt_am, `gueltig_bis`, `verbraucht_am`. Gültigkeit 48 h bzw. 30 min (`:23-24`). `beitragsreferenz(mitglied)` (`:82-86`) → `DDOE-<pk:04d>-<6 Hex aus SHA-256>`.

### uebersicht (`uebersicht/models.py`)
- **TagesZahl** (`datum` unique, `aufrufe`), **TagesBesucher** (`datum`, `kennung` 16 Hex; unique), **AntragAufruf** (antrag, datum, aufrufe; unique). `tageskennung(ip, browser, datum)` = SHA-256(`SECRET_KEY:datum:ip:browser`)[:16] (`:67-70`); `aufruf_zaehlen()` atomar via `F()`.

### anstoss (`anstoss/models.py`)
- **AnstossStatus**: neu/gesichtet/erledigt. **Anstoss** (`:20-43`): `text` (≤4000), `seite` (300), `nutzer` FK? (SET_NULL), `erstellt`, `status`, `vermerk` (300).

### mandatare (`mandatare/models.py`)
- `FOTO_HOECHSTGROESSE = 800_000` Bytes (`:23`); `foto_typ_erkennen()` prüft magische Bytes JPEG/PNG/WebP.
- **Mandat** (`:42-83`): mitglied (`mandate`), `bezeichnung` (120), `ebene` (Default gemeinde), `gebiet`, `angetreten`, `beendet` Date?, `kandidatur` FK Antrag? (SET_NULL — wird von keiner View gesetzt), `vorstellung` (≤2000), `foto` BinaryField, `foto_typ`; `aktiv`, `initialen`.
- **Aufgabenstatus**: offen/laufend/erledigt. **Aufgabe** (`:92-129`): mandat (`aufgaben`), `titel`, `beschreibung` (≤4000), `frist` Date?, `status`, `antrag` FK? (betreute Abstimmung), erstellt/aktualisiert; `ueberfaellig`.

### gremien (`gremien/models.py`)
- Konstanten (`:35-38`): `REVIEW_TAGE=14`, `UEBERARBEITUNG_TAGE=14`, `HOECHSTRUNDEN=3`, `ROLLEN_DAUER_TAGE=730`; `_registerzahl()` liest `parameter.zahl()` mit diesen Rückfällen.
- **Gremium** (`:49-53`): `expertenrat1` „Expertenrat — Gruppe 1 (Entwurf)", `expertenrat2` „Expertenrat — Gruppe 2 (Prüfung)", `koordinationsrat`, `integritaetsrat`.
- **Rolle** (`:56-99`): mitglied (`rollen`), gremium, `berufen_am`, `endet_am`, `bestaetigt` (MV), `beendet_grund` (200); `aktiv`, `aktive(gremium)`, `hat(mitglied, *gremien)`. `standard_ende()` = heute + Register `gremien-rollen-dauer-tage`.
- **EntwurfsStatus** (`:106-110`): in_arbeit / pruefung / unterstuetzer / angenommen.
- **Entwurf** (`:113-324`)
  - Felder: antrag OneToOne (`entwurf`), status, `runde` (Default 1), `vollzugsbezug` Bool, `eingereicht_am`, `review_frist`, `ueberarbeitung_frist`, erstellt_am.
  - `aktuelle_fassung()`; `einreich_stand()` (`:144-159`): einreichbar, wenn Ja ≥ ⌈aktive G1-Rollen / 2⌉ ∧ Ja > Nein ∧ Fassung vorhanden; `votum_stand()` (Annahmen, Rückgaben, Unterstützerzahl).
  - `haelt_beratung_offen()` (`:168-180`): PRUEFUNG/UNTERSTUETZER immer; IN_ARBEIT nur wenn `runde > 1` und die Überarbeitungsfrist läuft — ein nie eingereichtes Fenster hält nichts auf.
  - `einreichen()` (`:184-203`): Vollzugsbezug → PRUEFUNG, sonst UNTERSTUETZER mit `review_frist` = jetzt + Register `gremien-review-tage`; Audit `vorschlag_eingereicht`. `zu_den_unterstuetzern()` (`:205-209`). `zurueck_an_gruppe_1(grund, neue_runde, frist_erneuern)` (`:211-231`; neue Runde zählt hoch und setzt die Überarbeitungsfrist; Audit `vorschlag_zurueckgegeben`).
  - `_endabstimmung_oeffnen()` (`:233-276`): neue `AntragsFassung` mit Begründungspräfix „Vorschlag des Expertenrats, Runde N (§ 5 Abs 12).", Status ANGENOMMEN, Antrag → ABSTIMMUNG (`phase_beginn = jetzt`), Stimmberechtigte feststellen, Audit `phasenwechsel`.
  - `fortschreiben(antrag, jetzt)` (`:278-324`): bei UNTERSTUETZER — sobald alle Unterstützer gestimmt haben oder `review_frist` um: Rückgaben > Annahmen ∧ runde < Höchstrunden → neue Runde; sonst (auch bei Stille/Gleichstand) Endabstimmung. Bei IN_ARBEIT mit `runde > 1` und abgelaufener Überarbeitungsfrist → Endabstimmung mit der zuletzt vorgelegten Fassung.
- **EntwurfsFassung** (`:327-344`): entwurf (`fassungen`), nummer, wortlaut, begruendung, `verfasst_von`, erstellt_am; unique (entwurf, nummer); append-only.
- **EntwurfsBeitrag** (`:347-364`): entwurf (`beitraege`), mitglied, text (≤4000), `ki_lauf` FK? `ki.KILauf` (PROTECT), erstellt_am.
- **EinreichStimme** (`:367-380`): entwurf, mitglied, runde, `einverstanden` Bool; unique (entwurf, mitglied, runde).
- **Pruefung** (`:383-411`): entwurf (`pruefungen`), runde, `ergebnis` (validiert / zurueck / austausch), `begruendung` (≤4000), `durch`, erstellt_am, `korat_entscheid` (stattgegeben/abgelehnt/""), `korat_begruendung` (≤2000).
- **UnterstuetzerVotum** (`:414-431`): entwurf (`unterstuetzer_voten`), mitglied, runde, `annehmen` Bool, `wunsch` (≤2000), abgegeben_am; unique (entwurf, mitglied, runde). Offen (mit Namen).

### ki (`ki/models.py`, `ki/anbieter.py`)
- **Zweck**: nur `einschaetzung` „Einschätzung für die Gremien-Werkstatt" (`:24-25`).
- **KILauf** (`:28-71`, append-only): `zweck`, `antrag` FK? (SET_NULL, `ki_laeufe`), `angefordert_von` FK, `eingabe`, `antwort`, `anbieter`, `modell`, `tokens_ein`, `tokens_aus`, `dauer_ms`, `erfolgreich`, `fehler` (300), erstellt_am. `monatsverbrauch()` (Summe ein+aus im Kalendermonat), `monatsbudget()` (Register `ki-monatstokens`, Rückfall `DDOE_KI_MONATSTOKENS`).
- `steckplatz_stand()` (`:74-84`), `lauf_ausfuehren(zweck, auftrag, eingabe, mitglied, antrag)` (`:87-129`): kein Anbieter → `SteckplatzStumm`; Budget erschöpft → `SteckplatzStumm` (kein Lauf archiviert); Anbieterfehler → Lauf mit `erfolgreich=False` archiviert und `SteckplatzStumm`.
- `anbieter.py`: `MISTRAL_ENDPUNKT`, `ZEITGRENZE_SEKUNDEN=45`, `ANTWORT_HOECHSTTOKENS=900` (`:20-22`); `MistralAnbieter.frage()` (POST chat/completions, `temperature 0.2`, system=Auftrag, user=Eingabe, `:51-90`); `AttrappenAnbieter` (`:93-106`, Antwort „Attrappen-Einschätzung (kein echtes Modell): …"); `anbieter_waehlen()` (`:109-120`).

### parameter (`parameter/models.py`)
- **Parameter** (`:19-33`): `schluessel` Slug unique, `wert` (100), `einheit` (30), `beschreibung` (≤1000), `quelle` (200), `geaendert_am`. `zahl(schluessel, standard)` mit Rückfall bei DB-Fehler/unlesbar. **ERSTBESTAND** (`:46-87`): `gremien-review-tage=14`, `gremien-ueberarbeitung-tage=14`, `gremien-hoechstrunden=3`, `gremien-rollen-dauer-tage=730`, `ki-monatstokens=1000000`. `erstbestand_sicherstellen()` (get_or_create, überschreibt nie).

## 1.4 Policies

**`policies/grundordnung-v1.yaml`** (32 Zeilen): Liste von 2 Policies — `sachantrag-standard` v1 (Schwelle 3, Unterstützungsfrist 14 Tage, Beratung 21, Abstimmung 7, Mindestbeteiligung 0.05, Mehrheitsbasis `ja_nein`, Wiedereinbringungssperre 6 Monate; `:14-22`) und `sachantrag-dringlich` v1 (Schwelle 6, Frist 7 Tage, sonst gleich; `:24-32`). **Kein Code liest diese Datei** — es gibt keinen Import-Command; die aktive Verfahrensordnung kommt aus `demo_seed` (v2: Frist 60, Beratung 21, Abstimmung 28; `demo_seed.py:36-53`) bzw. Migration `0008_ordnung_v2_fristen` (`:12`, hebt eine bestehende v1 auf v2). Satzungsminima erzwingt `plattform_core/policy.py:16-18` (Beratung ≥ 21, Abstimmung ≥ 7, Beteiligung ≥ 0.05).

**`policies/kategorien-v2.yaml`** (2929 Zeilen, `version: 2`, `stand: 2026-08-20`): Schlüssel `lebensbereiche` → rekursive Knoten `{slug, name, beschreibung?, eurovoc?, schlagworte?, unterkategorien?}`. Struktur (per YAML ausgezählt): **312 Knoten, 6 Ebenen**: Ebene 1 = 1 Wurzel „Das gesellschaftliche Zusammenleben"; Ebene 2 = 4 Säulen („Sicherheit & Soziales Fundament", „Wirtschaft, Arbeit & Finanzen", „Lebensraum & Infrastruktur", „Bildung, Entwicklung & Gesellschaft"); Ebene 3 = 12 Bereiche (z. B. „Gesundheitswesen & Prävention", „Innere & Äußere Sicherheit", „Mobilität & Verkehr", „Kultur, Medien & Sport"); Ebene 4 = 24 Hauptkategorien der v1 (alle mit Schlagworten); Ebene 5 = 96; Ebene 6 = 175. Schlagworte tragen nur die Ebenen 4–6 (Kopfkommentar `:1-4`). `kategorien-v1.yaml` (295 Knoten, 3 Ebenen, 24/96/175) bleibt als Historie liegen; `kategorien_laden` nimmt die höchste Version (`kategorien_laden.py:27-30`).

## 1.5 `plattform_core` — Module und Algorithmen

- **`policy.py`**: `Policy` (frozen dataclass: id, version, unterstuetzung_schwelle, unterstuetzung_frist_tage, beratung_tage, abstimmung_tage, mindestbeteiligung, mehrheitsbasis=`ja_nein`|`abgegeben`, wiedereinbringung_sperre_monate=6); `__post_init__` erzwingt Minima; `als_dict()`, `aus_dict()` (unbekannte Felder → `PolicyFehler`).
- **`phases.py`**: `Phase` (unterstuetzung, beratung, abstimmung, angenommen, abgelehnt, verfallen, zurueckgewiesen, zurueckgezogen), `END_PHASEN`, `Transition(neue_phase, wirksam_ab, grund)`; `*_frist_ende()`; **`naechster_uebergang(phase, phase_beginn, jetzt, policy, unterstuetzungen, auszaehlung)`** (`:71-146`): UNTERSTUETZUNG → BERATUNG (Schwelle innerhalb Frist, `wirksam_ab=jetzt`; nach Frist rückwirkend zum Fristende) oder VERFALLEN; BERATUNG → ABSTIMMUNG zum Fristende; ABSTIMMUNG → ANGENOMMEN/ABGELEHNT zum Fristende (Auszählung Pflicht, sonst `ValueError`); `stimme_zulaessig()` (`:149-155`).
- **`tally.py`**: `Stimme` (ja/nein/enthaltung), `Auszaehlung` dataclass; `auszaehlen(stimmen, stimmberechtigte, policy)` (`:48-128`): Duplikat-Pseudonym → Fehler; Beteiligung per `Fraction` ohne Gleitkomma; Mehrheit `ja > nein` oder `2*ja > abgegeben`; Begründungstexte „Angenommen: …"/„Abgelehnt: …". `personenwahl_auszaehlen(zustimmungen, bewerbungen, stimmberechtigte, policy)` (`:160-231`): Reihung nach Zustimmungen absteigend, dann Einreichreihenfolge; Beteiligung = Pseudonyme mit ≥1 Zustimmung; Gewinner nur bei erreichter Mindestbeteiligung und > 0 Stimmen.
- **`eligibility.py`**: `Gegenstand` (sachfrage 3 Monate; personenwahl, mandatsnominierung, satzungsaenderung, aufloesung 12 Monate, `:31-37`); `monate_addieren()` mit Klemmung auf Monatsletzten; `stimmberechtigt(beitritt, gegenstand, stichtag, uebergang)`.
- **`hashchain.py`**: `GENESIS="0"*64`, `_kanonisch()` (sort_keys, keine Leerzeichen, UTF-8), `ereignis_hash(vorgaenger, ereignis)` = SHA-256(vorgänger ∥ kanonisch), `kette_pruefen(eintraege)` → (ok, erster fehlerhafter Index).
- **`similarity.py`**: `normalisieren()` (NFKC, casefold, Nicht-Wortzeichen → Leerraum), `trigramme()` (je Wort mit Randauffüllung `"  wort "`), `aehnlichkeit(a,b)` Jaccard, `aehnlichste(neuer_text, kandidaten, schwelle=0.18, limit=3)` (`:48-73`; Gleichstand → kleinere ID).
- **`klassifikation.py`**: `schlagwort_trifft()` (Einzelwort: Wort beginnt mit Schlagwort; Mehrwort: Wortfolge enthalten), `zuordnen(text, knoten, limit=3)` (`:37-84`): Punkte = getroffene unterschiedliche Schlagworte je Knoten; Vorfahren getroffener Nachfahren fliegen raus (tiefste Ebene gewinnt); Sortierung Punkte ↓, Tiefe ↓, ID ↑.
- **`faecher.py`** — `faecher_layout(zeilen, fokus_slug)` (`:41-156`)
  - Konstanten: `BREITE=1000` (x wird als Prozent ausgegeben, y in px), `ENKEL_HOECHSTZAHL=12`, Kürzung `KURZ_KIND=23`, `KURZ_ENKEL=16`, Anker 30 Zeichen (`:25-28`, `:99`).
  - Fokus: unbekannter/leerer Slug → erste Wurzel; leerer Baum → `{modus: boden, hoehe: 200, knoten: []}`. Tiefe = Weglänge zur Wurzel + 1; **Modus `mitte` ab Tiefe 3, sonst `boden`** (`:67`).
  - Dargestellt: **Anker (Fokus) in Schrift 24** (`:111`); dessen **Kinder in 22** auf einem Fächerbogen (`y = kinder_y + 22·(2t−1)²`; ab 4 Kindern jedes zweite zusätzlich 34 px tiefer, `:116-125`); dessen **Enkel in 20**, nur wenn 0 < Enkel gesamt ≤ 12 (drei Reihen à 30 px versetzt, `:127-135`); sonst zeigt jedes Kind `+n` (`mehr`, `:122`).
  - Modus `mitte`: **unter** dem Anker der klickbare Rückweg — höchstens die 2 nächsten Vorfahren (`weg[:2]`, Schrift 22 und 20) plus, bei längerem Weg, die Wurzel in 18 (`:84-89`, `:137-145`); Zwischenvorfahren ab Tiefe 5 werden übersprungen.
  - Zeilenraster (`:74-90`): Enkel y = 30/60/90; Kinder y = 166 (ohne Enkel 44); Anker y = Kinder + 96 (ohne Kinder 64); Weg-Stufen je 52 px; Höhe = Anker + 52·Stufen + 44. Fäden als (x1,y1,x2,y2) Anker↔Kind, Kind↔Enkel, Anker↔Weg.
  - **Fazit: maximal 3 Baumebenen mit/über dem Anker und maximal 3 Rückweg-Knoten darunter → höchstens 6 Ebenen gleichzeitig, nie mehr als 5 lückenlos** (Tiefe 1–2: 3 Ebenen; Tiefe 3: 5; Tiefe 4: 6; Tiefe ≥ 5: mit Lücke). Reale Wurzel (4 Säulen, 12 Bereiche): 17 Knoten, 3 Ebenen, Höhe 306 px; Tiefe 6 („Kassenärztliche Versorgung"): nur Anker + 3 Rückweg-Knoten (per Django-Shell verifiziert).
- **`weicherfilter.py`** (`VERSION=1`)
  - **8 Regler** (`REGLER`, `:23-32`): `gestimmt`, `unterstuetzt`, `entdeckungen`, `unterstuetzungsphase`, `abstimmungen`, `chronologisch`, `ablaufend`, `schwelle`; `HOECHSTWERT=100`.
  - `regler_bereinigen()` (ganzzahlig, [0,100], Unbekanntes 0), `ist_neutral()` (alle 0), `reihen(eintraege, werte)` (`:56-82`): Punkte = Σ Regler × Merkmal (Merkmal auf [0,1] geklemmt), Aufschlüsselung `anteile` je gesetztem Regler, stabile absteigende Sortierung (Gleichstand behält die Grundordnung).
  - **Merkmale** (gebaut in `verfahren/views.py:43-104`): `gestimmt` = |Kategorien des Antrags ∩ Kategorien aller Anträge mit eigenem Stimmregister-Eintrag| / |Kategorien des Antrags| (**richtungsunabhängig**); `unterstuetzt` analog mit eigenen Unterstützungen; `entdeckungen` = 1, wenn Kategorien vorhanden und keine im abonnierten Ast; `unterstuetzungsphase` / `abstimmungen` = 1/0 nach Phase; `chronologisch` = Altersrang (jüngster = 1); `ablaufend` = max(0, 1 − Resttage/60); `schwelle` = Unterstützungen/Schwelle (nur in der Unterstützungsphase).
  - Anzeigenamen `REGLER_NAMEN` (`views.py:31-40`): „Mehr, wo ich schon abgestimmt habe", „Mehr, wo ich unterstützt habe", „Entdeckungen außerhalb meiner Favoriten", „Mehr Unterstützungsphase", „Mehr laufende Abstimmungen", „Neues zuerst", „Bald ablaufende zuerst", „Knapp vor der Schwelle zuerst".
  - Voreinstellung: alle 0 (neutral). Profil-Limit 5 (`FilterProfil.HOECHSTZAHL`, durchgesetzt in `views_aktionen.py:321-336`). Regler-Schrittweite 5 (`parlament.html:38`).
- **`diagramme.py`**: Farben `BLAU #2C89B0`, `GOLD #D9A441`, `ROT #8C2B2B`, `TINTE #0E2230`, `GRAU #6b7a84` (`:18-23`); jedes SVG bringt eigenen Papiergrund `#FDFCF8` (`:36`); `linien_diagramm()` (Beschriftung nur am letzten Punkt, X-Labels erster/mittlerer/letzter), `balken_diagramm()` (Säulen mit Rundung, Label nur Maximum und letzter Wert), `anteils_balken()` (100-%-Balken mit 2 px Lücke, `<title>`-Tooltips). Kein JavaScript.
- **`beitraege.py`**: Referenz-Regex `DDOE[\s-]?(\d{4})[\s-]?([A-F0-9]{6})` (`:24`), `referenz_finden()` kanonisch, `name_passt()` (Nachname oder Vorname im Absender, Umlaute vereinfacht), `Eingang` dataclass, `umsatz_normalisieren()` (Berlin-Group-Felder, nur Gutschriften), `eingaenge_zuordnen()` (nur EUR, nur bekannte Referenzen).
- **`bankauszug.py`**: `camt053_lesen()` (nur `CRDT`-Einträge, Kennung AcctSvcrRef/NtryRef/TxId oder Fingerabdruck), `csv_lesen()` (Trenner-Erkennung, tolerante Kopfzeilen-Suche in `_SPALTEN`, deutsche Beträge/Daten, Fingerabdruck `csv-<sha256[:32]>`), `auszug_lesen()` (XML vs. CSV).

---

# 2. Seiten-Inventar (jede Route)

Legende Zugriff: **G** Gast · **M** angemeldetes Mitglied · **B** bestätigtes Mitglied (Identitätsstufe ≠ ungeprüft ∧ Status aktiv, `views_aktionen.py:54-68`) · **S** stimmberechtigt · **R:x** Rolle · **A** Admin (`hat_adminrechte`, `mitglieder/verwaltung.py:29-38`). Rendering aller Seiten über `verfahren/base.html`.

## 2.1 `verfahren` (`verfahren/urls.py`)

### `/` — `views.index` — `index.html` — G — GET
Kontext: Zahlen `mitglieder` (is_active-Konten), `laufend`, `beschluesse`; `wichtige` = laufende hervorgehobene Anträge `[:3]` (`views.py:184-192`). Abschnitte in Reihenfolge:
1. **Bühne `.held`**: Vorzeile „Direkte Demokratie Österreich", H1 **„Wir sind das Werkzeug."**, Untertitel („Hier entscheiden die Mitglieder selbst: …"), Knöpfe `.btn` „Mitglied werden" (→ `/mitgliedschaft/`) und `.btn-hell` „Das Parlament ansehen"; Zahlenband (3 `.zahl`): „bestätigte Mitglieder", „laufende Verfahren", „gefasste Beschlüsse" (`index.html:30-45`).
2. **„So funktioniert das System"** (`#so-funktioniert-es`), Erklärzeile, 5 nummerierte `.schritt` (CSS-Counter): „Einbringen", „Unterstützen", „Beraten", „Abstimmen", „Nachrechnen und umsetzen" (`:47-80`).
3. **„Die Wege durch die Plattform"** (`:82-188`): Erklärzeile „Zwei Antragswege, eine Werkstatt-Schleife — jede Frist ist ein offener Parameter." und ein Inline-SVG-Flussdiagramm (`role="img"`, viewBox 1240×448, `min-width:1120px`, horizontal scrollbar in `.fluss-rahmen`, das aus der 960-px-Textspalte ausbricht) mit drei Bahnen:
   - „SACHANTRAG": Antrag einbringen → Unterstützung „60 Tage · Schwelle" → Beratung „21 Tage" → Endabstimmung „28 Tage · geheim" → Beschluss „angenommen" → Umsetzungsregister „öffentlich · mit Verlauf" → Staatsapparat „Mandatare bringen ein / Vollzug · Prognoseabgleich".
   - „ENTWURFSSCHLEIFE · § 5 ABS 12": Expertenrat „erarbeitet den Vorschlag" → Prüfung Gruppe 2 „bei Vollzugsbezug · § 6 Abs 7" → Unterstützer-Votum „annehmen oder zurückgeben"; gestrichelter Pfeil „Werkstatt kann öffnen", Rückpfeil „zurück mit Wünschen · höchstens 3 Runden", Goldpfeil „angenommen — oder Frist um: Untätigkeit hemmt nie".
   - „MANDATS-KANDIDATUR · § 7 ABS 1": Kandidatur-Antrag „je Mandat einer — jederzeit" → Bewerbungen „jedes wählbare Mitglied" → Zustimmungswahl „geheim · mehreren zustimmbar" → Listenreihung des Wahlvorschlags „meiste Zustimmung führt die Liste an".
   - Zwei gestrichelte Gold-Plaketten „KI: Ähnlichkeitshinweis · Themen-Zuordnung" und „KI: Durchrechnung · Einschätzung in der Werkstatt" („gekennzeichnet und archiviert — sie schlägt vor, sie entscheidet nie").
   - **Die Fristen im Diagramm sind hart übersetzte Strings, nicht aus Policy oder Register.** Farben über CSS-Variablen (dark-mode-fähig).
4. **„Woran sich alles messen lässt"** (`#grundsaetze`): 4 `.grundsatz`-Kacheln „Nachrechenbar", „Keine verdeckte Reihung", „KI schlägt vor, entscheidet nie", „Ohne Hürden" (`:190-198`).
5. **„Alle Bereiche im Überblick"** (`#bereiche`): 8 Karten `.wk` (Das Parlament, Antrag einbringen, Mandatare, Gremien, Die Plattform in Zahlen, Umsetzungsregister, Die Zukunftswerkstatt, Die Mitgliedschaft) je mit H3, Text, „… →"; Hinweis „Die Plattform ist im Alpha-Betrieb: …" (`:200-246`).
6. **„Wichtige Abstimmungen"** (nur wenn vorhanden): Zeilen Titel + Phase, Erklärzeile „Hervorhebung nur durch begründeten, veröffentlichten Beschluss — nie durch einen Algorithmus." (`:248-259`).
Animationen: `.held`, `.wk`, `.schritt`, `.grundsatz`, `.fluss-rahmen` erscheinen mit `auftauchen` (gestaffelt bis 6. Kind, `base.html:275-280`); `.wk:hover` hebt 2 px (`index.html:8`). Keine htmx/Alpine.

### `/parlament/` — `views.parlament` — `parlament.html` (+ `_faecher.html`, `_kachel.html`, `_stern.html`) — G lesen, M handeln — GET (`?fach=<slug>`, `?suche=<text>`)
`main.breit` (max-width 1500 px). Für Gäste oben `.meldung.info` „Sie sehen das Parlament als Gast — mitlesen ist offen, mitentscheiden können Mitglieder." + Links Anmelden · Mitglied werden (`parlament.html:5-9`). Dann `div.parlament` (2-spaltiges Grid) mit 4 `section.feld`:

**Feld 1 `#feld-filter` „WeicherFilter"** (`parlament.html:14-87`)
- Kopf: H2 „WeicherFilter"; `.bk` rechts „Profil: <Name>" oder „Voreinstellung: neutral".
- Nur für M: `.filter-leiste` (feste Chip-Leiste, einfahrend von oben):
  - Chip **„Neutral"** — Formular POST `filter/neutral/` (`hx-post`, Feld tauscht sich selbst aus).
  - je gespeichertem Profil ein Chip (Tooltip „Profil anwenden") — POST `filter/<pk>/waehlen/`.
  - `<details class="regler-klappe">` mit `<summary class="chip">⚙ Regler</summary>` → `.regler-feld`: absolut positioniertes, halbtransparentes Overlay am rechten Feldrand (`right:8px; top:46px; bottom:8px; width:min(330px, 100%−16px); background:color-mix(karton 90 %, transparent); backdrop-filter:blur(6px)`, Animation `einfahren-rechts`; `base.html:146`, `:284`).
  - Im Overlay: Formular POST `filter/anwenden/` mit 8 `<label class="regler">` (`<span>Beschriftung <output>Wert</output></span>` + `<input type=range min=0 max=100 step=5 name="r_<key>" oninput=…>`), Knopf „Anwenden & speichern" (`.btn`), Textfeld `profilname` (Placeholder „Profilname", maxlength 40) + Knopf „Als neues Profil" (`name=als_neues`).
  - Hilfetext: „Offene Regel v1: Punkte = Regler × Merkmal, je Antrag aufgeschlüsselt und nachrechenbar. Ihr Profil wirkt nur auf Ihre eigene Ansicht — nie auf Reihung, Schwellen oder Ergebnisse (§ 2 Abs 6)."
  - Bei aktivem Profil zusätzlich Formular „Aktives Profil löschen" (`.btn-still`, POST `filter/<pk>/loeschen/`).
- Korpus bei aktivem, nicht-neutralem Profil: **gereihte Liste** — je Zeile Titel-Link, Ebenen-Badge bei regionalen Anträgen, Meta „<Phase> · seit dd.mm. · <Punkte> P" (`.punkte` mit `title`-Tooltip = Aufschlüsselung „<Reglername> <Anteil> · …" bzw. „neutral gereiht"), Stern-Include. Leerzustand: „Derzeit läuft kein Verfahren."
- Korpus sonst (**Grundordnung**, `views.py:321-334`): 4 Gruppen mit `.phasen-titel` „Laufende Abstimmungen", „In Beratung", „Sammeln Unterstützung", „Abgeschlossen" (angenommen/abgelehnt/verfallen, neueste 20); je Zeile Titel, Badge, Meta „<Phase> · seit dd.mm.", Stern (nur M).

**Feld 2 `#feld-favoriten` „Meine Favoriten"** (`parlament.html:90-125`)
- Kopf: H2 „Meine Favoriten" + Suchformular (GET, `role="search"`, `input[type=search] name=suche`, Placeholder „Lebensbereich suchen …", `hx-get` → tauscht nur dieses Feld).
- Mit `?suche=`: Trefferliste (max. 24, sortiert Tiefe/Name; Suche über Name, Beschreibung, Schlagworte, `views.py:195-217`) — je Zeile Name-Link `?fach=<slug>#feld-favoriten` (`hx-get`), Meta `pfad_kurz` + „ein laufendes Verfahren"/„N laufende Verfahren" (Ast-Summe), Stern-Formular Abo (M). Leerzustand „Nichts gefunden — andere Schreibweise oder ein Schlagwort versuchen." Fuß `.feld-fuss` „← Zurück zum Fächer".
- Sonst `_faecher.html`: `div.faecher` (Höhe aus Layout) mit `svg.faeden` (Fäden, `vector-effect=non-scaling-stroke`, `aria-hidden`) und je Knoten `span.fknoten.<rolle>.f<groesse>` (absolut, `left:x%; top:ypx`, `translate(-50%,-50%)`):
  - für M ein Stern-Formular POST `kategorien/<slug>/abonnieren/` (★ „Favorit entfernen" / ☆ „Als Favorit merken", `hx-post` → Feld);
  - der Anker als `span.fname` (nicht klickbar, Tooltip = voller Name);
  - alle anderen Knoten als `<a href="?fach=<slug>#feld-favoriten">` mit `hx-get`;
  - ggf. `a.mehr` „+n" (Tooltip „n Unterbereiche").
- Die Wurzel heißt im Fächer „Lebensbereiche" (`views.py:236-238`).

**Feld 3 `#feld-wichtig` „Wichtige Abstimmungen"** (`parlament.html:128-142`)
- Kopf: H2 + `.bk` „N · Beschluss des Integritätsrats".
- Korpus: `.kacheln` (2 Spalten) mit **allen** laufenden hervorgehobenen Anträgen (kein Limit, `views.py:266`) als `_kachel.html` mit `zeige_begruendung=True` (Hervorhebungsbegründung `truncatechars:110`) und Direktabstimmungs-Ziel `#feld-wichtig`.
- Leerzustand: „Derzeit ist nichts hervorgehoben."

**Feld 4 `#feld-region` „Meine Region"** (`parlament.html:145-166`)
- Kopf: H2 + `.bk` „Gemeinde · Bezirk · Land".
- Korpus: immer 3 Zeilen mit `.phasen-titel` „Gemeinde" / „Bezirk" / „Land" + „ · <Ort>", wenn der Wohnsitz bekannt ist; je Zeile `.kacheln.dreier` (3 Spalten, alle Treffer, kein Limit).
- Leerzustände: „Kein laufender Antrag in Ihrer Gemeinde — der erste kann Ihrer sein." / „Kein laufender Antrag in Ihrem Bezirk." / „Kein laufender Antrag in Ihrem Land." jeweils + Link „Antrag einbringen →".
- Gäste/ohne Wohnsitz: alle regionalen Anträge der Ebene. Für M ohne Wohnsitz: „Hinterlegen Sie Ihre Wohnsitz-Gemeinde im Profil, damit hier gezielt Ihre Region erscheint." (**Es gibt keine Profil-Seite für Mitglieder** — nur die Verwaltung kann den Wohnsitz ändern.)

**`_kachel.html`**
- Kopf `.kt`: Titel-Link + Stern (M).
- Meta: `.badge` Phase; `badge--hell` „Personenwahl" bei Mandat; Ort (`rest ort`, außer `ort_versteckt`).
- Stand je Phase: Unterstützung → `.balken` (Breite = % zur Schwelle) + „n von s Unterstützungen"; Beratung → „b Beitrag in der Beratung"/„b Beiträge in der Beratung"; Abstimmung → `.balken` + **„p % der Stimmberechtigten haben abgestimmt · Tendenz verdeckt bis Fristende"**.
- Resttage: „Frist endet heute" bzw. „noch **t** Tag(e)".
- Laufende Abstimmung, M: Sachantrag → `form.stimmreihe` POST `antrag/<pk>/abstimmen/` (`hx-post`, Ziel = Feld) mit „Ja" (`.btn-linie`), „Nein" (`.btn-linie`), „Enthaltung" (`.btn-still`); eigene Stimme mit `.gewaehlt` (Gold-Outline). Mandat → Link „Zur Wahl der Bewerbungen →".

**`_stern.html`**: Formular POST `antrag/<pk>/favorisieren/` mit `hx-post` + `hx-swap=outerHTML` (Ziel = das Formular selbst; View liefert bei `HX-Request` das Partial zurück, `views_aktionen.py:521-528`); ★/☆ mit Titel „Favorit entfernen"/„Als Favorit merken"; `weiter` = aktuelle URL.

htmx-Muster im Parlament: `hx-target="#feld-…" hx-select="#feld-…" hx-swap="outerHTML"` — die Views antworten mit Redirect auf `weiter`; htmx folgt dem Redirect, holt die ganze Seite und tauscht nur das Feld. **Flash-Messages werden dabei konsumiert, aber nicht angezeigt.**
CSS-Bewegung: Felder `auftauchen` gestaffelt (.06/.12/.18 s), Fächer `hineingleiten` .42 s, Filter-Leiste `einfahren-oben` .4 s, Regler-Overlay `einfahren-rechts` .32 s, Balken `wachsen` .8 s (Delay .25 s), Kachel-Hover −3 px + Gold-Kante, Stern-Hover scale 1.22 (`base.html:281-297`).

### `/einbringen/` — `views_aktionen.einbringen` — `einbringen.html` — B (login_required; 403-Seiten `nur_bestaetigte.html` / `mitwirkung_ruht.html`; 503 `keine_ordnung.html` ohne aktive VO) — GET/POST
- Kopf: Link „← Zum Parlament", H1 „Antrag einbringen", Absatz „Jedes bestätigte Mitglied kann Anträge einbringen (§ 5 Abs 1). Es gilt die Verfahrensordnung **<id> v<version>** — sie wird beim Einbringen eingefroren und gilt für diesen Antrag unverändert bis zum Ende (§ 5 Abs 5)." (`einbringen.html:5-9`).
- Formular `AntragsFormular` (`views_aktionen.py:71-142`):
  - Radio „Art des Antrags": „Sachantrag — ein Beschluss in der Sache" / „Mandats-Kandidatur — eine Personenwahl (§ 7 Abs 1): Mitglieder bewerben sich am Antrag, die meiste Zustimmung gewinnt, die Reihenfolge ergibt die Liste".
  - „Titel" (≤ 200; Hilfetext „Bei einer Mandats-Kandidatur: das Mandat, z. B. „Listenreihung Gemeinderat …"."), „Wortlaut des Antrags" (Textarea 10 Zeilen; Hilfetext zur Kandidatur), „Begründung" (optional, 6 Zeilen).
  - Radio „Gilt für": „Ganz Österreich" + je nach Profil „Mein Bundesland (…)", „Mein Bezirk (…)", „Meine Gemeinde (…)" — das Gebiet folgt zwingend dem Wohnsitz (`gebiet()`, `:133-142`). Lebensbereiche werden nicht gewählt, sondern automatisch zugeordnet.
- POST-Ablauf (`:163-220`): bei Sachantrag ohne `trotzdem` → `aehnlichste()` über „Titel Wortlaut" gegen alle offenen Anträge (Titel + aktueller Wortlaut). Treffer → dieselbe Seite mit `.karte.hinweis` **„Ähnliche Anträge gefunden"**: Text „Vielleicht gibt es Ihr Anliegen schon — dann ist eine Unterstützung wirksamer als ein Parallelantrag. **Der Hinweis ist ein Angebot, keine Hürde:** Sie können unverändert einbringen (§ 2 Abs 6 — kein Algorithmus entscheidet über Zulassung)."; je Treffer „Übereinstimmung ~p % · Phase: <Phase> · Beteiligung: n" + Titel-Link; Knöpfe „Trotzdem einbringen" (`name=trotzdem`, `.btn`) und „Lieber einen bestehenden Antrag unterstützen" (`.btn-still`, Link ins Parlament). Sonst Knopf „Antrag einbringen".
- Erfolg: `antrag_einbringen` + `kategorien_zuordnen`; Message „Ihr Antrag ist eingebracht und sammelt jetzt Unterstützung. Automatisch zugeordnet: <pfad_kurz, …>." (ohne Treffer ohne Zusatz) → Redirect zur Antragsseite. Audit `antrag_eingebracht`, `kategorien_zugeordnet`.
- Hilfetexte unter dem Formular: „Vor dem Einbringen gleicht die Plattform Ihren Text automatisch mit laufenden Anträgen ab (ADR-006). Der Hinweis schlägt vor — er blockiert nie. Titel und Wortlaut sind öffentlich." und „In Entwicklung (§ 5 Abs 10 lit d): die Folgenabschätzung — welche bestehenden Gesetze Ihr Antrag berühren würde und welche Branchen betroffen wären, berechnet mit der StaatsSimulation und stets als Modellrechnung gekennzeichnet." (`einbringen.html:43-48`).

### `/kategorien/`, `/kategorien/<slug>/` — `kategorie_weiter` — G — GET → 302 auf `/parlament/#feld-favoriten` bzw. `/parlament/?fach=<slug>#feld-favoriten` (`views_aktionen.py:580-587`). Altadressen-Weiterleitung.

### `/kategorien/<slug>/abonnieren/` — `kategorie_abonnieren` — M — POST: Abo umschalten; Messages „„X" ist jetzt Favorit — Neues daraus erscheint in Ihrem Hauptfenster." / „Favorit „X" entfernt."; Redirect `weiter` (nur interne Pfade) oder Parlament. Kein Audit.

### `/antrag/<pk>/` — `views.antrag_detail` — `antrag.html` — G lesen — GET (führt `antrag.fortschreiben()` aus; Middleware zählt Aufruf)
Reihenfolge (`antrag.html`):
- Link „← Zum Parlament"; H1 = Titel.
- Badge-Zeile (`:7-17`): `.badge` Phase; ggf. „Mandats-Kandidatur"; ggf. `badge--hell` „<Ebene> · <Gebiet>"; je Kategorie ein Link-Badge `pfad_kurz` (Tooltip „automatisch zugeordnet (F-47)") in den Fächer; „eingebracht dd.mm.yyyy von <Name> · Unterstützungen: n · Frist: dd.mm.yyyy, HH:MM"; für M Favoriten-Formular ★ „Favorit" / ☆ „merken" (**ohne htmx**).
- Hinweiskarte bei Hervorhebung: „**Vom Integritätsrat hervorgehoben (§ 5 Abs 10 lit b):** <Begründung | Begründung folgt.>"; bei Zurückweisung: „**Formal zurückgewiesen (§ 5 Abs 2):** …" (`:18-19`).
- H2 **„Wortlaut (Fassung N)"** als `<pre>`; H2 **„Begründung"** (nur wenn vorhanden) (`:21-23`).

Phasenvarianten (in dieser Reihenfolge im Template):
- **Unterstützung** (`:25-41`): H2 „Unterstützen". M: Formular POST `unterstuetzen/` mit „Diesen Antrag unterstützen" (`.btn`) bzw. „Sie unterstützen diesen Antrag." + „Unterstützung zurückziehen" (`.btn-still`). G: „Zum Unterstützen bitte anmelden oder Mitglied werden."
- **Kandidatur** (`art=mandat`, in allen Phasen, `:43-94`): H2 **„Bewerbungen (n)"** `#bewerbungen`.
  - Erklärtext in der Abstimmung: „Zustimmungswahl: Sie können mehreren Bewerbungen zustimmen und jede Zustimmung bis zum Fristende zurücknehmen. Zwischenstände werden nicht angezeigt — das Ergebnis erscheint erst nach Fristende."; sonst: „Wer sich beteiligt, wird hier als wählbar geführt. Die Bewerbung mit der meisten Zustimmung gewinnt die Wahl; die Zustimmungsreihenfolge ergibt die Reihung des Wahlvorschlags (§ 7 Abs 1)."
  - Je aktive Bewerbung: „**Name** · beworben am dd.mm.yyyy", in der Abstimmung für M Knopf „Zustimmen" (`.btn-linie`) bzw. „✓ Zugestimmt — zurücknehmen" (`.btn`) (POST `zustimmen/<bewerbung_pk>/`), darunter die Vorstellung.
  - Leerzustand: „Noch keine Bewerbung — die erste kann Ihre sein."; Hinweis „N Bewerbungen wurden zurückgezogen — dokumentiert im Audit-Log."; in der Abstimmung Hilfetext + Link „Prüfcode anzeigen" bzw. „Zum Zustimmen bitte anmelden."
  - Bis Abstimmungsbeginn: Bewerbungsformular (Textarea „Um dieses Mandat bewerben — Ihre Vorstellung (öffentlich)", Placeholder „Wer bin ich, wofür stehe ich, was bringe ich mit."; Pflicht-Checkbox „Ich bestätige, die gesetzlichen Voraussetzungen der Wählbarkeit zu erfüllen — insbesondere das passive Wahlalter (Art 26 Abs 4 B-VG, § 7 Abs 1 des Satzungsentwurfs)."; Knopf „Bewerbung einreichen") oder „Ihre Bewerbung steht — Sie werden als wählbar geführt." + „Bewerbung zurückziehen"; G: „Zum Bewerben bitte anmelden oder Mitglied werden."
  - Nach Ende (`:96-111`): H2 **„Ergebnis"** mit Begründungssatz, Tabelle Platz / Bewerbung / Zustimmungen (Gewinnerzeile fett + „gewählt"), „Beteiligung x / y Stimmberechtigte", „Nachrechnen: Stimmliste exportieren · Meine Stimme in der Liste finden".
- **Abstimmung** (Sachantrag, `:113-128`): H2 **„Abstimmen"**. M: ggf. „Ihre aktuelle Stimme: **Ja** — Sie können sie bis zum Fristende ändern. Prüfcode anzeigen"; Formular „Ja" (`.btn`), „Nein" (`.btn-linie`), „Enthaltung" (`.btn-still`); Hilfetext „Ihre Stimme wird unter einem Pseudonym erfasst und nach Abstimmungsende in der nachrechenbaren Stimmliste veröffentlicht (§ 5 Abs 3 lit c)." G: „Zum Abstimmen bitte anmelden." **Keine Zwischenstände.**
- **angenommen/abgelehnt** (Sachantrag, `:130-137`): H2 **„Ergebnis"**: Begründung fett, „Ja n · Nein n · Enthaltung n · Stimmberechtigte n", „Nachrechnen: Stimmliste exportieren und mit `verify/nachrechnen.py` unabhängig prüfen (§ 5 Abs 8). Meine Stimme in der Liste finden."
- **angenommen** zusätzlich (`:139-171`): H2 **„Umsetzung"** `#umsetzung`: Status-Badge (Ampelfarben `vollzug_stil`) + „Öffentlicher Stand der Umsetzung (§ 6 Abs 10) — geführt im Umsetzungsregister."; Historie je Eintrag (Badge, Datum, Name, Vermerk); Leerzustand „Noch kein Vollzugseintrag."; für A Formular „Stand fortschreiben (Verwaltung)": Select Status, Textarea „Öffentlicher Vermerk (Stand, Hindernis, nächster Schritt, Termin)", Knopf „Eintrag veröffentlichen", Hinweis „Dauerhaft und auditiert — Einträge werden nie geändert oder gelöscht."
- **verfallen**: nur Badge, keine Ergebnis- oder Aktionskarte.
- **Entwurfsschleife** (wenn ein `Entwurf` existiert, `:173` → `gremien/_schleife.html`): H2 **„Entwurfsschleife** § 5 Abs 12".
  - `in_arbeit` Runde 1: „Der Expertenrat erarbeitet zu diesem Antrag einen Vorschlag (Runde 1). Das hält nichts auf: Läuft die Beratungsfrist ab, geht der Antrag regulär weiter."; Runde > 1: „Der Vorschlag wurde mit Wünschen an den Expertenrat zurückgegeben — Überarbeitung läuft (Runde N)." + „Ohne neue Einreichung bis … geht die zuletzt vorgelegte Fassung zur Endabstimmung — Untätigkeit hemmt nie."
  - `pruefung`: „Der Vorschlag des Expertenrats hat Vollzugs- oder Beschaffungsbezug und liegt der zweiten, getrennt besetzten Gruppe zur Prüfung vor (§ 6 Abs 7) — ihr Ergebnis wird samt Begründung veröffentlicht."
  - `unterstuetzer`: Fristsatz „Frist: … Danach wertet der Fristablauf aus — bleibt es still, geht der Vorschlag zur Endabstimmung."; `.phasen-titel` „Der Vorschlag im Wortlaut" (`<pre>`) + Begründung; „Voten bisher: j annehmen, n zurückgeben — von u Unterstützern. Offen geführt:" + Liste (**Name** — annehmen/zurückgeben, Wunsch); für Unterstützer Formular POST `/gremien/votum/<entwurf_id>/`: Textarea „Bei Rückgabe: Was soll anders werden?", Knöpfe „Vorschlag annehmen" (`.btn`) / „Mit Wunsch zurückgeben" (`.btn-linie`), eigenes Votum mit ✓; Nicht-Unterstützer: „Das Votum steht den Unterstützern dieses Antrags zu — Unterstützung ist öffentlich, das Votum auch."
  - `angenommen`: „Der Vorschlag des Expertenrats (Runde N) wurde als neue Antragsfassung übernommen — darüber wird abgestimmt (§ 5 Abs 3 lit d)."
  - Abschnitt „Prüfungen der Gruppe 2 (§ 6 Abs 7 — Begründungen öffentlich)": Ergebnis, Runde, Datum, Begründung, ggf. „Koordinationsrat: <entscheid> — <Begründung>".
- **Beratung** (immer, `:175-195`): H2 **„Beratung (n)"**; Karte mit Kommentaren (Kopf „Name · dd.mm.yyyy, HH:MM", Text `linebreaksbr`); Leerzustand „Noch keine Beiträge."; in Unterstützung/Beratung für M Formular: Textarea „Beitrag zur Beratung" (maxlength 4000, required) + Hinweis „Sachlich und respektvoll — Beiträge erscheinen unter Ihrem Anzeigenamen." + Knopf „Beitrag veröffentlichen" (`.btn-linie`); G: „Zum Mitberaten bitte anmelden."
- Abschluss (`:197-198`): H2 **„Eingefrorene Verfahrensregeln (§ 5 Abs 5)"** mit `<pre>`-JSON des Policy-Snapshots.

Keine htmx auf der Antragsseite; einzige Bewegung: `.karte` `auftauchen`. Kein Chat, keine KI-Zone, keine Grafiken.

### Aktionen am Antrag (alle POST, `login_required`, Redirect zurück)
- `/antrag/<pk>/unterstuetzen/` (B): Umschalter; nur in Unterstützungsphase („Die Unterstützungsphase dieses Antrags ist beendet."); danach `fortschreiben()` (Schwelle). Messages „Danke — Ihre Unterstützung ist erfasst." / „Ihre Unterstützung wurde zurückgezogen." Kein Audit-Eintrag für Unterstützungen.
- `/antrag/<pk>/kommentieren/` (B): nur Unterstützung/Beratung („Die Beratung dieses Antrags ist beendet."); `KommentarFormular` max 4000; „Ihr Beitrag ist veröffentlicht." Kein Audit.
- `/antrag/<pk>/abstimmen/` (M, **S** per `ist_stimmberechtigt(SACHFRAGE, phase_beginn.date(), uebergang)` → sonst 403 `nicht_stimmberechtigt.html`): Mandat → Fehler „Bei einer Mandats-Kandidatur stimmen Sie den einzelnen Bewerbungen zu."; `stimme_abgeben` (Audit `stimme`/`stimme_geaendert`); Message „Ihre Stimme ist erfasst — bis zum Fristende können Sie sie ändern."; Redirect auf `weiter` (interne Pfade) oder Antrag.
- `/antrag/<pk>/bewerben/` (B + S PERSONENWAHL, `waehlbar`-Checkbox Pflicht): Audit `bewerbung`/`bewerbung_erneuert`.
- `/antrag/<pk>/bewerbung-zurueckziehen/` (M): Audit `bewerbung_zurueckgezogen`.
- `/antrag/<pk>/zustimmen/<bewerbung_pk>/` (M + S PERSONENWAHL): Umschalter; Audit `personenwahl_stimme`.
- `/antrag/<pk>/favorisieren/` (M): Umschalter; bei HX-Request Partial `_stern.html`, sonst Redirect.
- `/antrag/<pk>/vollzug/` (A, sonst 403 `verwaltung_kein_zugang.html`): `vollzug_fortschreiben`; Audit `vollzug`.
- `/antrag/<pk>/export.json` (G, GET): erst nach Ende (sonst 409 `{"fehler": …}`); JSON `{antrag, titel, art, policy, stimmberechtigte, stimmen[{pseudonym, stimme}], exportiert_am}` + bei Mandat `bewerbungen[]`, `zustimmungen[]`; `Content-Disposition attachment antrag-<pk>-export.json`.
- `/antrag/<pk>/meine-stimme/` — `eigene_stimme.html` (M, sonst Redirect Login): H1 „Meine Stimme prüfen", Karte „Ihre Stimme zu **<Titel>** steht unter diesem Pseudonym in der öffentlichen Stimmliste:" + `<code class="gross">` Pseudonym-Hex, Hinweis „Notieren Sie sich den Code. …"; Leerzustand „Für diesen Antrag ist von Ihnen keine Stimme erfasst." (**Der `pruefcode` des Registers wird nicht angezeigt — nur das Pseudonym.**)

### Filter-Aktionen (M, POST, Redirect auf `weiter`/Parlament)
`/filter/anwenden/` (Regler aus `r_<name>`; `als_neues` + `profilname` → neues/überschriebenes Profil; sonst aktives Profil bzw. Auto-Profil „Eigenes"; Limit 5 → „Höchstens fünf Profile — bitte zuerst eines löschen oder überschreiben."; Erfolg „Ihr Filter „X" ist aktiv — die Reihung folgt jetzt Ihren offenen Reglern."), `/filter/<pk>/waehlen/`, `/filter/neutral/` (alle inaktiv), `/filter/<pk>/loeschen/` („Profil „X" gelöscht."). Kein Audit (rein persönlich).

### `/zukunftswerkstatt/` — `views.zukunftswerkstatt` — `zukunftswerkstatt.html` — G — GET
- Bühne `.held`: Vorzeile „Werkzeug zur rekursiven Optimierung der gesamtgesellschaftlichen Selbstorganisation", H1 **„Die Zukunftswerkstatt"**, Untertitel „Was würde Ihr Antrag im Staat bewegen? Die Zukunftswerkstatt beantwortet das — mit der StaatsSimulation als Rechenkern, einem digitalen Zwilling des Staatswesens in wachsender Auflösung: …" (`zukunftswerkstatt.html:6-14`).
- H2 „Zwei Gesichter, ein Werkzeug": 2 `.bereich` „Nach außen / Politische Bildung durch Selbermachen" und „Nach innen / Das Sinnesorgan der Selbstregulation" (`:16-34`).
- H2 „Vier Grundsätze": 4 `.bereich` „Die KI schlägt vor, sie entscheidet nie.", „Der Demos darf atmen, die Stimme wiegt immer gleich.", „Das Gedächtnis ist der Schatz, nicht das Modell.", „Auf Unwissen gebaut." (`:36-62`).
- H2 „Der Weg eines Antrags im Zielbild": 6-Punkte-Liste (Einbringen · Unterstützung · Beratung · Vorlage an die Unterstützer · Endabstimmung · Umsetzung und Lernen) + Hinweis „Die Schritte 1 bis 3 und 5 bis 6 entsprechen dem Satzungsentwurf 2.3; die Unterstützer-Schleife in Schritt 4 ist Zielbild und erhält vor Einführung einen eigenen Satzungsbaustein." (`:64-85`).
- H2 „Woher die Fakten kommen" (RIS, Personal-Aggregate, Vergabedaten — Prosa) (`:87-95`); H2 „Wer wacht worüber" (Koordinationsrat, Integritätsrat, Expertenrat in zwei Gruppen, Beanstandung durch Mitglieder — Prosa) (`:97-106`).
- H2 **„Der Modell-Steckplatz — Rechenschaft in Zahlen"** `#steckplatz` (`:108-131`): Erklärtext; „**Angeschlossen:** <anbieter> · <modell>" oder „**Derzeit ist kein Anbieter angeschlossen** — der Steckplatz ist ehrlich leer; die Oberflächen sagen das an Ort und Stelle."; „Archivierte Läufe: n · Tokens diesen Monat: v von b · Alle offenen Parameter →"; `.phasen-titel` „Zuletzt archivierte Läufe" (8: „<Zweck> — <Antrag>", Meta „<Modell> · ein+aus Tokens · ms · Datum · gescheitert: <Fehler>").
- H2 „An die demokratischen Bewegungen weltweit" (`.karte.hinweis`): Einladungstext, „Schreiben Sie uns: plattform@ddoe.at · Quellcode auf GitHub · Zur Partner-Seite →" (`:133-146`).
- Schlusshinweis „Einschätzungen der Zukunftswerkstatt sind politische Bildung, keine Rechtsberatung. Die vollständige Strategie mit allen Ausbaustufen ist Teil des offenen Lastenhefts im Quellcode-Repository." (`:148-149`).
- **Alles außer dem Steckplatz-Block ist Zielbild-Prosa** — Rechtsfolgen-, Aufwand- oder Prognose-Register existieren im Code nicht.

### `/staatssimulation/` — `RedirectView` permanent → `/zukunftswerkstatt/` (`verfahren/urls.py:41-45`).

### `/partner/` — `views.partner` — `partner.html` — G — GET
- H1 **„Internationale Partner"**; Erklärzeile „Das Labor der Demokratien — die Einladung an die verwandten Bewegungen weltweit (§ 12 des Satzungsentwurfs)." (`partner.html:5-6`).
- Karte: Strategietext („… Unser Schlüssel zur Zusammenarbeit ist die Trennung von **System** und **Parametern**: …") (`:8-15`).
- H2 **„Der Fahrplan der Zusammenarbeit"**: 3 `.zeile` „1 · Software bereitstellen und gemeinsam einrichten", „2 · Gemeinsame Standards und Werkzeuge", „3 · Lernfortschritt und Parameter gemeinsam erheben", je mit Erläuterung (`:17-31`).
- H2 **„Was wir mitbringen — und was wir suchen"**: zwei Absätze „**Wir bringen mit:** …" / „**Wir suchen:** …" (`:33-41`).
- Hinweiskarte: „**Sprechen wir.** Kurz und konkret — mit einem Angebot und einer Lernfrage. Arbeitssprache Englisch oder Deutsch."; `.btn` **„Kontakt aufnehmen"** (`mailto:plattform@ddoe.at`), `.btn-linie` „Quellcode ansehen" (GitHub); „In Vorbereitung: das Partner-Konto mit eigener Rolle und Oberfläche auf dieser Plattform — das Rollen-Fundament dafür steht bereits." (`:43-50`).
- Keine Konto-Anlage, keine Bestätigung, keine Partner-Rolle im Code.

### `/umsetzung/` — `views.umsetzung` — `umsetzung.html` — G — GET (`?status=`)
- H1 **„Umsetzungsregister"**; Erklärabsatz „Ein Beschluss, den niemand umsetzt, entwertet das Verfahren. Deshalb steht hier zu jedem angenommenen Antrag der Stand der Umsetzung — öffentlich, fortlaufend und mit voller Geschichte (§ 6 Abs 10). Geführt wird das Register vom Integrations- und Berichtswesenrat; bis dieses Organ besetzt ist, schreiben die Admins der Plattform fort. …" (`umsetzung.html:5-10`).
- Filterzeile: Knöpfe „alle" + je Status „<offen|in Umsetzung|blockiert|umgesetzt|zurückgestellt> (n)"; aktiver Filter als `.btn-linie` (`:12-18`).
- Je angenommenem Antrag `.karte`: Status-Badge (Ampelfarbe), Titel-Link, „angenommen am dd.mm.yyyy", letzter Vermerk, „Stand vom dd.mm.yyyy, HH:MM · <Name> · ganze Geschichte (n)" oder „Noch kein Vollzugseintrag — der Stand gilt als offen." (`:20-34`).
- Leerzustand „Noch keine angenommenen Anträge — sobald der erste Beschluss fällt, beginnt hier seine Umsetzungsgeschichte."; Fuß „Register als JSON exportieren — maschinenlesbar, mit voller Historie (F-23)."

### `/umsetzung.json` — `umsetzung_json` — G — JSON `{register:[{antrag, titel, angenommen_am, status, historie[]}], exportiert_am}`, Attachment `umsetzungsregister.json`.
### `/gesund/` — `gesund` — G — `{"status":"ok"}` (Healthcheck; von Besuchszählung ausgenommen).

## 2.2 `mitglieder` (`mitglieder/urls.py`)

### `/mitgliedschaft/` — `views.mitgliedschaft` — `mitgliedschaft.html` — G — GET
- Bühne `.held`: Vorzeile „Mitglied werden", H1 **„Ihre Stimme, direkt."**, Untertitel („Als Mitglied der DDÖ entscheiden Sie selbst: …"), Knöpfe „Jetzt Mitglied werden" (`.btn` → `/mitglied-werden/`) und „So entsteht ein Beschluss" (`.btn-hell` → `#weg`) (`mitgliedschaft.html:6-16`).
- H2 **„Was Sie als Mitglied können"**: `.kat-grid` mit 6 `.karte`, je 64×64-Inline-SVG-Ikone (`role="img"` + `aria-label`: „Ein Blatt mit Stift", „Drei erhobene Hände", „Zwei Sprechblasen", „Ein Stimmzettel fällt in eine Urne", „Eine Lupe über einer Ergebnisliste", „Ein Zahnrad mit Häkchen") + H2 (19 px) + Text: „Anträge einbringen", „Unterstützen", „Mitberaten", „Abstimmen", „Selbst nachrechnen", „Umsetzung verfolgen" (`:18-78`).
- H2 **„Vom Antrag zum Beschluss"** `#weg`: `.fluss` mit 6 `.station` (nummerierte Kreise 28 px, verbindende Gradient-Linie Deep→Gold, letzte Station goldener Punkt — reines CSS, `base.html:220-226`): „Einbringen", „Unterstützung sammeln", „Beratung" („Mindestens 21 Tage öffentliche Erörterung. Die Zukunftswerkstatt rechnet durch, welche Gesetze berührt wären und was die Umsetzung kosten würde; der Expertenrat gibt seine Stellungnahme im Wortlaut ab."), „Abstimmung", „Ergebnis — nachrechenbar", „Umsetzung" (`:80-121`).
- H2 **„Die Mitgliedschaft im Detail"**: `.hf-grid--zwei` mit 4 `.bereich`: „Anwartschaft / Mitentscheiden mit Anlauf" (3 bzw. 12 Monate, Übergangsregel), „Beitrag / Selbsteinschätzung statt Tarif" („Richtwert 30 € im Jahr …"), „Identität / Ein Mensch, eine Stimme — pseudonym nach außen", „Ihre Region / Gemeinde, Land, Bund" (`:123-153`).
- Karte „Ihr Werkzeug dazu / Die Zukunftswerkstatt": Absatz „Zu jedem Antrag liefert die Zukunftswerkstatt — mit der StaatsSimulation als Rechenkern — eine mit amtlichen Quellen belegte Einschätzung: berührte Gesetze, Aufwand für den Staatsapparat, Ausschreibungspflichten, realistische Dauer. …" + Link „Wie die Zukunftswerkstatt arbeitet →" (`:155-163`).
- Hinweiskarte **„Bereit, mitzuentscheiden?"** („Die Registrierung dauert zwei Minuten: Name, E-Mail, Wohnsitz-Gemeinde — Bestätigungslink, fertig. …") mit „Jetzt Mitglied werden" (`.btn`) + „Ich bin schon Mitglied — anmelden" (`.btn-linie`) (`:165-173`).

### `/mitglied-werden/` — `views.registrieren` — `registrieren.html` — G — GET/POST
- H1 „Mitglied werden", Absatz („Mitglied kann jede natürliche Person ab dem vollendeten 16. Lebensjahr werden, …") + Link „Was die Mitgliedschaft bedeutet →".
- Formular `RegistrierungsFormular` (`views.py:39-102`, mit `BotschutzMixin`):
  - „Vorname", „Nachname" (≤ 80), „E-Mail-Adresse" (Duplikat → „Für diese Adresse existiert bereits ein Konto — nutzen Sie den Login per E-Mail-Link."), „Geburtsjahr" (< 16 Jahre → „Mitglied kann werden, wer das 16. Lebensjahr vollendet hat (§ 4 Abs 1).").
  - „Wohnsitz-Gemeinde": Textfeld mit `<datalist id="gemeinden">` „Name (Bezirk)" aller Gemeinden (Placeholder „Tippen und aus der Liste wählen …"; Hilfetext „Bitte aus dem amtlichen Gemeindeverzeichnis wählen — Bezirk und Bundesland ordnen wir dann automatisch zu (F-43). Mit der ID Austria erfolgt das später amtlich."); Validierung `Gemeinde.finden`; Fehler „Diesen Gemeindenamen gibt es mehrmals — bitte präzisieren: …" / „„X" steht nicht im amtlichen Gemeindeverzeichnis — bitte aus der Liste wählen. Meinten Sie: …?".
  - Checkbox „Ich bekenne mich zu den Grundsätzen des § 3 des Satzungsentwurfs (ein Mensch – eine Stimme, Menschenwürde, Rechtsstaat, Gewaltfreiheit)."
  - Botschutz (`botschutz.py`): verstecktes Honigtopf-Feld `website`, signierter Zeitstempel `pruefung` (Mindestzeit 5 s, Gültigkeit 1 h), **Rechenfrage als verzerrtes SVG-Bild** mit Feld „Sicherheitsfrage: Ergebnis der Rechnung im Bild" und Hilfe „Bitte lösen Sie die kleine Rechnung im Bild. Falls Sie das Bild nicht lesen können, schreiben Sie uns an didide@ddoe.at — wir helfen sofort."; Fehler „Die Sicherheitsfrage wurde nicht richtig beantwortet — bitte erneut versuchen."; IP-Drossel 5/h („Zu viele Versuche von dieser Verbindung — bitte in einer Stunde erneut.").
  - Knopf „Beitritt beantragen"; Hilfetext „Sie erhalten eine E-Mail mit einem Bestätigungslink (gültig 48 Stunden). Erst mit der Bestätigung wird Ihr Zugang aktiv — ein Passwort gibt es bewusst nicht: …".
- Datenkarte „**Was mit Ihren Daten geschieht:** Name, Geburtsjahr und Gemeinde dienen der Mitgliederverwaltung … Keine Weitergabe, keine Werbung."
- POST (`:109-163`): Konto mit `username=email`, `is_active=False`, `set_unusable_password`; Gemeinde/Bundesland/Wohnsitz aus dem Verzeichnis; Bestätigungsmail innerhalb `transaction.atomic` (Mailfehler → Rollback + „Ihre Registrierung wurde nicht gespeichert: <MAIL_STOERUNG>"); Audit `registrierung`; Antwort `mail_gesendet.html`. Das Geburtsjahr wird **nicht gespeichert** (nur validiert).

### `/bestaetigen/<token>/` — `bestaetigen` — G — GET: Token einlösen (48 h), `is_active=True`, `beitritt=heute` (falls leer), Login, Audit `email_bestaetigt`, Message „E-Mail bestätigt — willkommen! Sie sind jetzt Anwärterin bzw. Anwärter." → Redirect `/einfuehrung/1/`; ungültig → 400 `token_ungueltig.html` (H1 „Dieser Link ist nicht mehr gültig", Knopf „Neuen Anmeldelink anfordern").

### `/willkommen/` — `willkommen` — `willkommen.html` — M (sonst Redirect Login) — GET
H1 „Willkommen, <Vorname>!", Anwartschafts-Absatz; H2 **„Mitgliedsbeitrag — am schnellsten per QR-Code"** → Include `_beitrag_kasten.html`: EPC-QR-SVG (`segno`, Empfänger „Direkte Demokratie Österreich", IBAN `AT57 2033 0000 0006 9435`, Betrag `EUR30`, Verwendungszweck „<Referenz> Mitgliedsbeitrag", `views.py:182-213`), Anleitung „**Banking-App öffnen → „Zahlen mit Code" → diesen Code scannen.** …", „Richtwert: **30 € im Jahr** — Selbsteinschätzung …", „Oder von Hand: Empfänger … IBAN … Verwendungszweck <DDOE-0001-XXXXXX>", Hinweis zur Referenz; H2 „Was Sie jetzt schon können".

### `/einfuehrung/<1|2|3>/` — `einfuehrung.einfuehrung` — `einfuehrung.html` — M (login_required; andere Zahl → 404) — GET
- Kopfzeile: H1 je Schritt „Willkommen! Finden Sie Ihre Lebensbereiche" / „Ihre erste Abstimmung" / „So bringen Sie einen Antrag ein" + Knopf „Einführung überspringen" (`.btn-still` → Parlament) (`einfuehrung.html:6-13`).
- `.schrittleiste` (`aria-label="Fortschritt der Einführung"`): Punkte 1–3 (✓ für erledigte, `.aktiv` für aktuellen), Beschriftungen „Lebensbereiche", „Abstimmen", „Antrag", danach „€ Beitrag" (`:15-22`).
- **Schritt 1** (`:24-66`): Karte mit Baum-SVG („Ein Baum mit Sternen an den Ästen") und Text „Alles Leben in Österreich ist hier in einem Baum geordnet — von einer Wurzel über vier Säulen bis in die einzelne Tätigkeit. **Favorisieren Sie, was Sie betrifft** (der Stern ☆): Neues daraus erscheint dann auf Ihrer Startseite unter „Meine Favoriten". Ein Stern auf einer Säule oder einem Bereich umfasst den ganzen Ast. …"; Zähler „Sie haben bereits n Favoriten gewählt."; `.kat-grid` der 4 Säulen (Kinder der Wurzel, `einfuehrung.py:30-36`) mit Link in den Fächer, Stern-Formular (weiter = Schritt 1), Beschreibung (`truncatechars:120`), Link „Genauer wählen: hinein in die Bereiche →"; Hilfetext „Sie können auch gezielt suchen — zum Beispiel Ihren Beruf oder Ihr Anliegen: auf der Seite <a href="/kategorien/">Lebensbereiche</a> oben rechts. Mit dem Zurück-Knopf Ihres Browsers kommen Sie hierher zurück." (**veraltet — `/kategorien/` ist nur noch ein Redirect in den Fächer**); Knopf „Weiter: Ihre erste Abstimmung →".
- **Schritt 2** (`:68-100`): Urnen-SVG; Text „Abgestimmt wird mit **Ja, Nein oder Enthaltung** — mindestens sieben Tage lang, und bis zum Fristende können Sie Ihre Stimme ändern. Ihre Stimme steht unter einem **Pseudonym** in der öffentlichen Stimmliste; mit Ihrem persönlichen Prüfcode finden Sie sie dort wieder …"; Hinweis „Ansehen können Sie alles sofort — Ihre erste eigene Stimme folgt, sobald Ihr Konto nach dem Beitragseingang freigeschaltet ist."; Hinweiskarte „**Gerade läuft zum Beispiel:**" mit der ersten laufenden Abstimmung (hervorgehobene zuerst, `einfuehrung.py:38-42`) + „Diese Abstimmung ansehen →"; Knöpfe „← Zurück", „Weiter: Antrag einbringen →".
- **Schritt 3** (`:102-128`): SVG („Ein Antrag und ähnliche Anträge daneben"); Text zum Ähnlichkeitshinweis („… Einbringen bleibt trotzdem immer möglich — kein Algorithmus entscheidet über Zulassung. Die passenden Lebensbereiche ordnet die Plattform automatisch zu."); „Der Weg danach: Unterstützung sammeln → mindestens 21 Tage öffentliche Beratung → mindestens 7 Tage Abstimmung → dauerhaft veröffentlichtes, nachrechenbares Ergebnis."; Knöpfe „← Zurück", „Alle Anträge ansehen" (`.btn-linie`), „Zum Abschluss: Ihr Mitgliedsbeitrag →" (`.btn` → `/willkommen/`).

### `/anmelden/` — `login_anfordern` — `login.html` — G — GET/POST
H1 „Anmelden", Text „Kein Passwort — Sie erhalten einen Anmeldelink per E-Mail (gültig 30 Minuten). Sobald die ID Austria angebunden ist, ersetzt sie diesen Schritt."; `LoginFormular` (E-Mail + Botschutz-Rechenbild), Knopf „Anmeldelink senden"; Drossel 10/h. Antwort immer `mail_gesendet.html` (H1 „Bitte sehen Sie in Ihr Postfach", keine Adress-Enumeration); Mailfehler → `MAIL_STOERUNG`.
### `/anmelden/<token>/` — `login_einloesen` — G — GET: Login, Redirect `/parlament/`; sonst 400 `token_ungueltig.html`.
### `/abmelden/` — `abmelden` — POST — Message „Sie sind abgemeldet." → `/`.

### `/beitrag/` — `beitraege_views.beitrag` — `beitrag.html` — M (Redirect Login) — GET
H1 **„Mitgliedsbeitrag"**, Absatz; `_beitrag_kasten.html`; Karte **„Schon überwiesen?"** (Text je nach Bankkopplung) + Formular „Ich habe überwiesen" (POST `/beitrag/gemeldet/`); H2 **„Ihre verbuchten Beiträge"** (letzte 10: Datum, Betrag €; „Nur Sie und die Verwaltung sehen diese Liste — Beitragshöhen sind privat.").
### `/beitrag/gemeldet/` — `beitrag_gemeldet` — M — POST: ohne Kopplung Info „Danke für die Meldung! Der automatische Abgleich ist noch nicht eingerichtet — …"; Mindestabstand 180 s („Der letzte Abgleich ist keine drei Minuten her — …"); sonst `bank.abgleich_ausfuehren()`, Messages je Ergebnis (Treffer: „Ihre Zahlung ist angekommen und verbucht — Ihre Mitwirkungsrechte sind aktiv. Danke!"; Kontingent; Bank nicht erreichbar; „Noch kein Eingang mit Ihrer Referenz sichtbar — …").

### `/verwaltung/` — `verwaltung.liste` — `verwaltung_liste.html` — A (sonst 403 `verwaltung_kein_zugang.html` „Nur für die Verwaltung"; nur deutsch) — GET (`?q=`, `?status=`)
- H1 „Mitgliederverwaltung"; Einleitung „Zugang nur für Admins. Jede Handlung hier wird im öffentlichen Audit-Log festgehalten — mit Aktion und Begründung, ohne persönliche Daten."; Links „Beiträge & Bank →", „Anstöße →", „Mandatare →", „Gremien-Rollen →" (Parameter-Verwaltung fehlt).
- 3 `.bereich`: „Bestand" (n Konten gesamt · aktiv · unbestätigt), „Ruhend" (pausiert (Beitrag ausständig) · ausgeschlossen), „Suche" (Textfeld Placeholder „Name, E-Mail, Gemeinde …" + Select „alle Status"/Status + Knopf „Filtern").
- Tabelle (max. 200, neueste zuerst): Mitglied (Klarname, Badge „Admin"), E-Mail, Gemeinde, Identität (gekürzt), Status („unbestätigt" bei inaktiv), Beitritt, „öffnen →"; Leerzustand „Keine Treffer."
### `/verwaltung/<pk>/` — `verwaltung.mitglied` — `verwaltung_mitglied.html` — A — GET/POST
- Kopf: H1 Name; Badges (unbestätigt (E-Mail offen) / aktiv / pausiert / ausgeschlossen; „fixer Admin" / „Admin"); „Mitglied #n · beigetreten … · letzter Beitrag …"; Karte „**Begründung des Status:** …".
- H2 „Stammdaten korrigieren": `StammdatenFormular` (`verwaltung.py:47-92`): Vorname, Nachname, E-Mail-Adresse, „Öffentlicher Anzeigename (leer = Klarname)", „Wohnsitz-Gemeinde (leer = keine Angabe)" mit Datalist, Identitätsstufe (Select, 4 Stufen), „Letzter Beitragseingang" (Date) → „Speichern"; Hinweis „Bezirk und Bundesland folgen automatisch dem Gemeindeverzeichnis. Die Änderung wird (ohne Werte) im Audit-Log vermerkt."; Audit `verwaltung/stammdaten_geaendert` mit Feldliste; ohne Änderung „Keine Änderungen.".
- H2 „Beitrag": Knopf „Beitragseingang heute vermerken" (hebt Pause auf; Hinweis „Bis zum Kontoauszug-Import händisch.").
- H2 „Status": Textarea „Begründung (wird im Audit-Log veröffentlicht)" + Knöpfe „Pausieren (Beitrag ausständig)", „Ausschließen (vollzieht den Beschluss nach § 4 Abs 6)", „Wieder aktiv setzen"; Regeln (`verwaltung.py:168-221`): fixer Admin unantastbar („Der satzungsgebende Erstzugang ist unantastbar (F-51)."), nicht auf sich selbst, Grund Pflicht für Pausieren/Ausschließen. Hilfe „Pausiert: Anmelden und Lesen bleiben möglich, Mitwirkungsrechte ruhen. Ausgeschlossen: Konto wird deaktiviert. Beides ist umkehrbar („Wieder aktiv setzen")."
- H2 „Adminrechte": „Zum Admin ernennen" / „Adminrechte entziehen" / „Immer Admin (DDOE_FIX_ADMIN) — nicht entziehbar." Jede Aktion → Audit `verwaltung/<aktion>` (+ Grund).
### `/verwaltung/beitraege/` — `verwaltung_beitraege` — `verwaltung_beitraege.html` — A — GET (löst ggf. nachholenden Bankabgleich aus, ≥ 6 h Abstand, `beitraege_views.py:112-115`)
- H1 „Beiträge & Bank"; Einleitung (F-59) + „← zur Mitgliederliste".
- Karte „Bankkopplung": ohne Schlüssel „Die Dienst-Schlüssel fehlen noch: `DDOE_BANK_SECRET_ID` und `DDOE_BANK_SECRET_KEY` …"; gekoppelt „**Gekoppelt.** Zustimmung gültig bis … · heute n/4 Abrufe · zuletzt abgeglichen …" + Hinweis (180 Tage) + Knopf „Jetzt abgleichen"; sonst Bank-Select (Liste des Dienstes) oder Textfeld „Institutions-ID (z. B. SPARKASSE_OOE_ASPKAT2L)" + „Konto koppeln →".
- Karte „Kontoauszug hochladen": Text (George-Export, camt.053-XML oder CSV), Datei-Upload (`.xml,.csv,.txt,.camt`, ≤ 5 MB) + „Auszug abgleichen".
- H2 „Prüfhinweise" (bis 20 Eingänge mit Namensabweichung: Datum · Mitglied-Link · Betrag).
- H2 „Länger als 12 Monate ohne Eingang": Tabelle (Checkbox, Mitglied, E-Mail, Letzter Eingang/„noch nie", Status) + Knöpfe „Ausgewählte erinnern" / „Alle n erinnern"; Hilfe „… ausgeschlossen wird wegen des Beitrags niemand (§ 4 Abs 3)."; Leerzustand „Niemand fällig — alle Eingänge liegen innerhalb der letzten zwölf Monate."
- H2 „Zuletzt verbucht" (15, Badge „Namenshinweis").
### `/verwaltung/beitraege/erinnern/` (A, POST): Erinnerungsmail (Text hart im Code, Link `DDOE_BASIS_URL/beitrag/`), Audit `beitrag_erinnerung` mit Anzahl. `/verwaltung/beitraege/auszug/` (A, POST, Upload → `auszug_lesen` → `verbuchen_aus_umsaetzen`, Audit `auszug_abgleich`). `/verwaltung/beitraege/abgleichen/` (A, POST, erzwungener API-Abgleich). `/verwaltung/bank/koppeln/` (A, POST → Redirect zum GoCardless-Link). `/verwaltung/bank/rueckkehr/` (A, GET, schließt Kopplung ab, Consent 180 Tage).

## 2.3 `uebersicht` — `/uebersicht/` — `views.index` — `uebersicht.html` — G — GET
- H1 **„Die Plattform in Zahlen"**; Einleitung („Was sich hier tut — offen für alle, laufend aktualisiert. Abstimmungsverhalten erscheint ausschließlich als Summe je Abstimmung: …").
- 3 `.bereich`: „Mitglieder" (Zahl 34 px + „bestätigte Mitglieder · n neu in den letzten 7 Tagen"), „Anträge" („laufende Anträge von g insgesamt · n neu diese Woche"), „Besuche" („Seitenaufrufe heute · b Besucher:innen heute · w Aufrufe in 7 Tagen").
- H2 „Anträge nach Phase": in Unterstützung / in Beratung / in Abstimmung / angenommen / abgelehnt (verfallen fehlt).
- H2 **„Abstimmungen: Ergebnisse und Beteiligung"**: je Abstimmung (laufende und entschiedene, sortiert nach `-phase_beginn`) eine Karte mit Badge („Abstimmung läuft" / angenommen / abgelehnt), 100-%-SVG-Balken Ja/Nein/Enthaltung, Legende „■ Ja n · ■ Nein n · ■ Enthaltung n — a von b Stimmberechtigten (p % Beteiligung)" — **auch für laufende Abstimmungen** (Widerspruch zum „Tendenz verdeckt"-Grundsatz der Kacheln). Leerzustand „Noch keine Abstimmungen."
- H2 **„KI-Verbrauch des Modell-Steckplatzes"**: Anbieter + Modell oder „— / kein Anbieter angeschlossen", „archivierte Läufe", „davon gescheitert (bleiben dokumentiert)", „Tokens diesen Monat: v von b · p%", `.balken`-Meter (6 px, `role="img"`, aria-label „Monatsbudget zu p Prozent verbraucht"), Hinweis „Ist das Budget erschöpft, wird der Steckplatz stumm bis zum Monatswechsel." + Links „Läufe im Detail →", „Budget im Parameterregister →".
- H2 „Entwicklung": 3 Karten mit SVG-Diagrammen — „Mitglieder im Zeitverlauf (kumuliert)" (Linie), „Neue Anträge je Woche (letzte 8 Wochen)" (Säulen), „Seitenaufrufe je Tag (letzte 30 Tage)" (Säulen); Leerzustand „Noch keine Daten."
- H2 „Meistgelesene Anträge" (Top 5 nach `AntragAufruf`; Leerzustand „Noch keine gezählten Aufrufe — die Zählung beginnt mit dieser Version.").
- Hinweiskarte „**Wie hier gezählt wird:** ohne Cookies, ohne Werbe-Skripte und ohne Speicherung von IP-Adressen. … Der Prototyp enthält zur Veranschaulichung einige Demo-Daten."

## 2.4 `anstoss`
- **Widget `_widget.html`** (Include in `base.html:367`, auf jeder Seite, im Druck ausgeblendet): `div.anstoss-ecke` (fixed rechts unten, `base.html:257`) mit
  - Bestätigungsblase `#anstoss-blase` (`role="status"`): „✓ Danke — Ihre Meldung ist gespeichert." + X (`aria-label="Bestätigung schließen"`); `hidden` außer bei `?anstoss=danke`;
  - `<details class="anstoss-fleck">` (offen bei `?anstoss=warte|leer`): `<summary>` (`aria-label="Anstoß geben — Feedback und Wünsche"`) mit Sprechblasen-SVG + „Anstoß" als goldene Pille; `.anstoss-karte` (absolut, `bottom:54px`, Breite min(340px, 100vw−36px)) mit X-Link (`aria-label="Schließen"`, `onclick` schließt; ohne JS Reload von `request.path`), H3 „Anstoß geben", Text „Was fehlt? Was stört? Was wünschen Sie sich? Jede Nachricht wird gespeichert und fließt in die Weiterentwicklung der Plattform ein.", `#anstoss-echo` (`aria-live="polite"`) mit Server-Fehlertexten „Bitte einen Moment warten, bevor Sie den nächsten Anstoß senden." / „Bitte schreiben Sie zuerst eine Nachricht.";
  - Formular POST `/anstoss/` (`hx-post`, `hx-target="#anstoss-echo"`, `hx-swap="innerHTML"`, `hx-on::after-request` → `reset()`): hidden `seite` (= `request.get_full_path`), Honigtopf `webseite` (`.honig`, `tabindex=-1`, `aria-hidden`), Textarea `text` (Placeholder „Ihr Anliegen — so konkret wie möglich", maxlength 4000, required), Hinweis „Wird Ihrem Konto zugeordnet, damit wir nachfragen können." (M) / „Sie schreiben anonym — als Mitglied könnten wir nachfragen." (G), Knopf „Absenden".
- **`/anstoss/`** — `senden` — G/M — POST (`anstoss/views.py:32-65`): `seite` nur interne Pfade; Session-Drossel (60 s Abstand, 20/Tag); Honigtopf gefüllt → stilles Verwerfen mit Erfolgsanzeige; leer → „leer"; speichert `Anstoss(text[:4000], seite, nutzer?)`. Bei HX-Request → `_meldung.html` (bei „danke" Inline-`<script>`, das `<details>` schließt und die Blase einblendet; sonst Fehlertext ins Echo), sonst Redirect `<seite>?anstoss=<ergebnis>`. Kein Audit.
- **`/verwaltung/anstoesse/`** — `verwaltung.html` — A — GET (`?status=`): H1 „Anstöße"; Bereiche „Bestand" (gesamt · neu · gesichtet · erledigt), „Filter" (alle / je Status), „Export" (`anstoesse.csv` · `anstoesse.json`); Tabelle (max. 300, neueste zuerst): Wann / Anstoß (`pre-wrap`, + „Vermerk: …") / Seite (`<code>`) / Von (Name oder „anonym") / Status-Badge / Knöpfe „gesichtet", „erledigt", „wieder öffnen" (POST `/verwaltung/anstoesse/<pk>/status/`, Redirect auf `weiter`). Leerzustand „Noch keine Anstöße (mit diesem Status)." **Kein Eingabefeld für `vermerk`** — die View liest `vermerk` aus POST, das Template sendet keines.
- `/verwaltung/anstoesse/export.csv` (UTF-8-BOM; Felder nr, erstellt, seite, status, mitglied, vermerk, text) und `/verwaltung/anstoesse/export.json` — A, GET.

## 2.5 `mandatare`
- **`/mandatare/`** — `views.liste` — `liste.html` — G — GET:
  - H1 **„Mandatare"**, Erklärzeile („Die Mandatar-Steuerung (§ 7 Abs 9 des Satzungsentwurfs): Jeder Mandatsträger der DDÖ zeigt hier öffentlich seine aktuellen Aufgaben und Entscheidungsprozesse samt Fristen — … das freie Mandat bleibt unberührt (§ 7 Abs 2).").
  - Mit aktiven Mandaten: `.mandatar-karten` (auto-fill ≥ 260 px), je Karte rundes Foto 72 px (`/mandatare/<pk>/foto`) oder Initialen-Platzhalter (Nacht-Verlauf, Serif), Name-Link, „Bezeichnung · Gebiet|Ebene", „seit MM/YYYY", bis zu 2 offene/laufende Aufgaben mit „Frist: dd.mm.yyyy" (überfällig `.spaet` rot/fett), Leerzustand „Derzeit keine offenen Aufgaben eingetragen.", Link „Aufgaben und Abstimmungen ansehen →"; darunter „n beendete Mandate bleiben dokumentiert."
  - Ohne Mandate: Karte „**Noch hält die DDÖ kein öffentliches Mandat.** Ehrlich gesagt: Genau dafür bauen wir — und die Wahl unserer Kandidaten läuft bereits, hier auf der ParlamentPlattform (nicht im österreichischen Parlament: Die DDÖ reiht ihre eigenen Wahlvorschläge)."; `.phasen-titel` „Laufende Mandats-Kandidaturen" (bis 6; **Phase als roher Slug `{{ a.phase }}`**, `liste.html:53`); Leerzustand „Derzeit läuft keine Mandats-Kandidatur — jedes Mitglied kann eine einbringen."; Knöpfe „Mitglied werden" (`.btn`), „Kandidatur-Antrag einbringen" (`.btn-linie`).
- **`/mandatare/<pk>/`** — `detail.html` — G: „← Alle Mandatare"; Foto 120 px / Initialen; H1 Name; Badges Bezeichnung + Gebiet; „seit dd.mm.yyyy", ggf. „**beendet am …**"; Vorstellung; H2 **„Aktuelle Aufgaben und Entscheidungsprozesse"** + Erklärzeile; je Aufgabe (sortiert: offen/laufend zuerst, nächste Frist vorn) Status-Badge, Titel, „Frist: …" (überfällig markiert), Beschreibung, „Zur betreuten Abstimmung: <Titel> →"; Leerzustand „Noch keine Aufgaben eingetragen."; Karte „Richtschnur und Rechenschaft: Die Ergebnisse der betreuten Abstimmungen sind Beschlusslage (§ 7 Abs 4 und 5); …" + „Zum Umsetzungsregister →".
- **`/mandatare/<pk>/foto`** — Binärantwort mit `foto_typ` (Cache 1 h), 404 ohne Foto.
- **`/verwaltung/mandatare/`** — `verwaltung.html` — A (nur deutsch): H1 „Mandatare"; Karte „Mandat anlegen" (`MandatFormular`: Mitglied, Mandat, Ebene, Gebiet, Angetreten am, Vorstellung (öffentlich)) → „Mandat anlegen"; je Mandat Karte mit Foto-Upload (JPEG/PNG/WebP ≤ 800 kB, „Foto speichern"), „Mandat beenden", H3 „Aufgaben" mit Statusknöpfen „laufend" / „erledigt" / „wieder öffnen", Formular „Aufgabe veröffentlichen" (Titel, Beschreibung (öffentlich), Frist, „Antrags-Nr. (betreute Abstimmung, optional)"); Leerzustände „Noch keine Aufgaben." / „Noch kein Mandat angelegt."
- `/verwaltung/mandatare/aktion/` (POST, `mandatare/views.py:113-192`): Aktionen `anlegen` (Audit `mandat_angelegt`), `beenden` (Audit `mandat_beendet`), `foto` (Magic-Bytes-Prüfung; Fehler „Bitte ein Bild bis 800 kB wählen (JPEG, PNG oder WebP)." / „Dateityp nicht erkannt — …"; **kein Audit**), `aufgabe` (Audit `mandats_aufgabe`), `aufgabe_status` (**kein Audit**).

## 2.6 `gremien`
- **`/gremien/`** — `uebersicht.html` — G: H1 **„Gremien"**, Erklärzeile (§ 6 Abs 8); `.hf-grid` mit 4 Bereichen je Gremium (Name, je Rolle „Name / berufen … · bis … · von der Mitgliederversammlung bestätigt|Bestätigung ausständig"; Leer „Noch unbesetzt — die Ausschreibung läuft über den Koordinationsrat."); „n beendete Rollen bleiben mit Grund dokumentiert."; „Im Alpha-Betrieb besetzt die Verwaltung die Rollen von Hand — …".
- **`/gremien/mein/`** — M: Redirect je Rolle (G1 → Werkstatt, G2 → Prüfung, KoRat → Koordination, sonst `/gremien/`). **Integritätsrat hat kein Ziel.**
- **`/gremien/expertenrat/`** — `expertenrat.html` — R:expertenrat1 (Admins lesend; sonst 403 `kein_zugang.html` „Nur für Rolleninhaber"): H1 **„Expertenrat — Werkstatt (Gruppe 1)"**, Aufsichtshinweis „Du siehst diesen Bereich als Aufsicht (Verwaltung) — schreiben können nur aktive Rolleninhaber."; Karte „Anträge in der Beratung" (Sachanträge in Beratung, Link ins Fenster, Meta Status · Runde N · „Vollzugsbezug — Gruppe 2 prüft" oder „Noch kein Entwurfsfenster geöffnet."; Leer „Derzeit ist kein Sachantrag in der Beratung."); Karte „Zuletzt übergebene Entwürfe" (10).
- **`/gremien/expertenrat/<antrag_id>/`** — `fenster.html` — R:expertenrat1 (Admins lesend; nur Sachanträge, sonst 404):
  - Kopf: „← Zurück zur Werkstatt", H1 **„Entwurfsfenster: <Titel>"**; 2-spaltiges `.fenster-grid` (1fr 1.2fr, ≤ 900 px einspaltig).
  - **Links** Karte „Der Antrag daneben": „Antragsfassung N · Zur öffentlichen Antragsseite →", Wortlaut (`pre-line`), Begründung.
  - **Rechts ohne Entwurf**: „**Noch kein Entwurfsfenster.** Beim Öffnen wird der Antragswortlaut als Fassung 1 übernommen — die Arbeitsgrundlage." + Knopf „Entwurfsfenster öffnen" (nur in Beratung + Rolle) bzw. „Der Antrag ist nicht (mehr) in der Beratung."
  - **Rechts mit Entwurf**, Karte „Stand der Schleife": Pillen `.stand-punkt` Status, „Runde N", ggf. „Vollzugsbezug — Gruppe 2 prüft"; Fristsätze „Die Unterstützer haben bis … Zeit — danach wertet die Frist aus (Untätigkeit hemmt nie)." bzw. „Überarbeitung bis … — ohne neue Einreichung geht die zuletzt vorgelegte Fassung zur Endabstimmung."; „Voten: j annehmen, n zurückgeben — von u Unterstützern."
  - Karte „Fassungen (alles bleibt stehen)": „Fassung N · <Verfasser> · <Zeit>", Wortlaut, Begründung; `<details>` „Eine frühere Fassung"/„n frühere Fassungen"; Formular (nur `in_arbeit` + Rolle) „Neue Fassung anhängen" (Textarea vorbefüllt mit letzter Fassung, Feld „Was wurde geändert, und warum?", Knopf „Fassung anhängen").
  - Karte „Einreichen (interne, dokumentierte Abstimmung)" (nur `in_arbeit` + Rolle): „Stand: j Ja, n Nein — nötig sind k Ja bei a aktiven Rollen."; Knöpfe „Ja, einreichen" / „Noch nicht" (eigene Stimme mit ✓); Checkbox „Unmittelbarer Vollzugs- oder Beschaffungsbezug — dann prüft Gruppe 2 vor den Unterstützern (§ 6 Abs 7)." + „Merken"; bei Einreichbarkeit Knopf „Vorschlag jetzt einreichen" (`.btn`).
  - Karte „Interne Beratung (dokumentiert, § 6 Abs 9)": Beiträge (Name, Zeit, Text; KI-Beiträge mit `.ki-kennzeichen` „KI-Vorschlag · <Modell>", Tooltip „Aus dem Modell-Steckplatz — jeder Lauf steht im öffentlichen Archiv."); Leerzustand „Noch keine Beiträge."; Formular Textarea (Placeholder „Beitrag zur internen Beratung …") + „Beitrag festhalten"; Formular „KI-Einschätzung einholen" mit Hinweis „Sie schlägt vor, sie entscheidet nie — der Lauf wird archiviert (F-60)." bzw. „Derzeit ist kein Anbieter angeschlossen — der Steckplatz ist ehrlich leer."
- **`/gremien/expertenrat/<antrag_id>/aktion/`** — POST, nur aktive G1-Rolle (`gremien/views.py:160-276`); Aktionen:
  - `oeffnen` (nur Beratung; legt `Entwurf` + Fassung 1 „Arbeitsgrundlage: übernommener Antragswortlaut." an; Audit `entwurfsfenster_geoeffnet`),
  - `fassung` (append, Wortlaut Pflicht; „Neue Fassung angehängt — alle früheren bleiben stehen."),
  - `beitrag` („Beitrag festgehalten."),
  - `ki_einschaetzung` (`lauf_ausfuehren` mit `EINSCHAETZUNGS_AUFTRAG`; Eingabe = „Titel: …" + „Entwurfsfassung N (Runde R):" + Wortlaut + „Begründung der Fassung:" + „Wünsche der Unterstützer aus der Vorrunde:" als Liste; Antwort wird `EntwurfsBeitrag` mit `ki_lauf`; bei `SteckplatzStumm` Info-Message mit dem Grund),
  - `vollzugsbezug` (Checkbox → „Vollzugs-/Beschaffungsbezug: ja — Gruppe 2 prüft / nein."),
  - `stimme` (Einreich-Stimme je Runde, update_or_create),
  - `einreichen` (prüft `einreich_stand`; Fehler „Noch nicht einreichbar: j Ja bei k nötigen Stimmen (aktive Rollen: a)."; Erfolg „Eingereicht — der Vorschlag geht zuerst an Gruppe 2 (§ 6 Abs 7)." bzw. „Eingereicht — der Vorschlag liegt jetzt den Unterstützern vor (§ 5 Abs 12).").
  - Bei eingereichtem Vorschlag blockiert: „Der Vorschlag ist eingereicht — die Werkstatt ruht, bis er zurückkommt."
- **`/gremien/votum/<entwurf_id>/`** — POST, M, nur Unterstützer des Antrags, nur Status `unterstuetzer`; Rückgabe braucht Wunsch („Eine Rückgabe braucht einen konkreten Wunsch — sonst weiß die Werkstatt nicht, wohin."); danach `antrag.fortschreiben()` (sofortige Auswertung, wenn alle Unterstützer gestimmt haben). Kein eigener Audit-Eintrag (Audit erst beim Rückgabe-/Phasenwechsel).
- **`/gremien/pruefung/`** — `pruefung.html` — R:expertenrat2 (Admins lesend):
  - H1 **„Expertenrat — Prüfung (Gruppe 2)"**, Aufsichtshinweis wie oben.
  - Karte „Zur Prüfung vorgelegt": je Entwurf mit Status `pruefung` Titel-Link, „Runde N", voller Wortlaut der aktuellen Fassung; falls ein Austauschantrag offen ist: „Ein Austauschantrag liegt beim Koordinationsrat — seine Entscheidung steht aus."; sonst Formular Textarea „Begründung (wird veröffentlicht)" (required) + Knöpfe **„Validieren"** (`.btn`) / **„Begründet zurückgeben"** / **„Austausch beim KoRat beantragen"** (`.btn-linie`). Leerzustand „Derzeit liegt kein Vorschlag zur Prüfung vor."
  - Karte „Zuletzt abgeschlossene Prüfungen" (10: Titel, Ergebnis, Datum).
  - `/gremien/pruefung/<entwurf_id>/aktion/` (POST, `views.py:430-462`): Pflicht Ergebnis + Begründung („Bitte Ergebnis wählen und begründen — die Begründung wird veröffentlicht."); Audit `vorschlag_geprueft`; `validiert` → `zu_den_unterstuetzern()` („Validiert — der Vorschlag liegt jetzt den Unterstützern vor (§ 5 Abs 12)."); `zurueck` → `zurueck_an_gruppe_1(frist_erneuern=True)` („Mit Begründung zurückgegeben — die Werkstatt ist wieder am Zug."); `austausch` → Entwurf bleibt in Prüfung („Austausch beantragt — der Koordinationsrat entscheidet; die Begründung ist öffentlich.").
- **`/gremien/koordination/`** — `koordination.html` — R:koordinationsrat (Admins lesend):
  - H1 **„Koordinationsrat"**, Aufsichtshinweis.
  - Karte „Offene Austauschanträge": je Prüfung Titel-Link, „Runde N · Datum", „*Begründung der Gruppe 2:* …", Formular Textarea „Begründung der Entscheidung (wird veröffentlicht)" + Knöpfe „Stattgeben — Gruppe 1 austauschen" (`.btn`) / „Ablehnen" (`.btn-linie`), Hinweis „Stattgeben beendet alle aktiven Rollen der Gruppe 1 — mit dokumentiertem Grund; der Entwurf geht an die neu besetzte Gruppe."; Leerzustand „Kein Austauschantrag offen."; darunter die letzten 10 entschiedenen („Austauschantrag stattgegeben/abgelehnt · Begründung").
  - Karte „Aktive Rollen": alle aktiven Rollen aller Gremien (Name — Gremium, „bis dd.mm.yyyy", „Bestätigung ausständig"); Leerzustand „Keine aktiven Rollen."; Hinweis „Berufen und beenden läuft im Alpha-Betrieb über die Verwaltung — der Ausschreibungsablauf wächst in die Plattform."
  - Karte **„Zukunftswerkstatt-Posteingang"** — reiner Platzhalter: „Hier landen künftig die Vorschläge aus der Zukunftswerkstatt zur Sichtung durch den Koordinationsrat — samt KI-Vorprüfung als Vorschlag, nie als Entscheidung (Ring 0b)." + „Zur Zukunftswerkstatt →".
  - `/gremien/koordination/<pruefung_id>/aktion/` (POST, `views.py:499-543`): ein einzelnes Mitglied entscheidet sofort (**keine interne KoRat-Abstimmung**); Pflicht Entscheid + Begründung; `stattgegeben` → alle aktiven G1-Rollen enden mit Grund „Austausch durch den Koordinationsrat (§ 6 Abs 7)", Entwurf zurück an Gruppe 1 (Frist erneuert) („Stattgegeben — n Rollen der Gruppe 1 beendet (dokumentiert); die neue Gruppe übernimmt den Entwurf."); `abgelehnt` → „Abgelehnt — der Vorschlag bleibt bei Gruppe 2 zur Prüfung."; Audit `austausch_entschieden`.
- **`/verwaltung/rollen/`** — `verwaltung_rollen.html` — A (nur deutsch):
  - H1 „Gremien-Rollen", Einleitung (§ 6 Abs 8) + Links `/gremien/` und „← Mitgliederverwaltung".
  - Karte „Rolle berufen": `RollenFormular` (Mitglied (aktive Konten), Gremium (4 Choices), „Endet am" (Default heute + Register-Dauer 730 Tage), Checkbox „Von der Mitgliederversammlung bestätigt (§ 6 Abs 8)") → „Berufen".
  - Karte „Alle Rollen": je Rolle Name — Gremium, „berufen … · bis …", Zustand „beendet: <Grund>" / „abgelaufen" / „MV-bestätigt" / „Bestätigung ausständig"; für aktive Rollen Knöpfe „MV-Bestätigung vermerken" und Textfeld „Grund (bleibt dokumentiert)" + „Beenden"; Leerzustand „Noch keine Rollen berufen."
  - `/verwaltung/rollen/aktion/` (POST): `berufen` (Audit `rolle_berufen` mit Gremium, Enddatum, bestätigt), `bestaetigen` (Audit `rolle_bestaetigt`), `beenden` (Grund Pflicht: „Eine vorzeitige Beendigung braucht einen Grund — er bleibt dokumentiert."; Audit `rolle_beendet`).

## 2.7 `parameter`
- **`/parameter/`** — `liste.html` — G (legt Erstbestand an!): H1 **„Offene Parameter"**, Erklärzeile, Link „Register als JSON →"; je Parameter „**schluessel** = wert einheit / beschreibung / Quelle: … · Stand: dd.mm.yyyy"; Leer „Noch keine Parameter eingetragen."
- **`/parameter.json`** — G — `{parameter:[…]}` mit `Access-Control-Allow-Origin: *`.
- **`/verwaltung/parameter/`** — `verwaltung.html` — A (nur deutsch): je Parameter Textfeld Wert + Pflichtfeld „Grund (wird veröffentlicht)" + „Ändern". `/verwaltung/parameter/aktion/` (POST): Audit `parameter_geaendert {schluessel, alt, neu, grund}`. **Nicht aus der Mitgliederverwaltung verlinkt** (nur `/verwaltung/` → Beiträge, Anstöße, Mandatare, Gremien-Rollen).

## 2.8 `i18n/` — `django.conf.urls.i18n` (`set_language`, POST aus dem Header-Formular).

## 2.9 Fehler-/Sperrseiten
`nur_bestaetigte.html` (403; H1 „Noch einen Schritt", Text mit Link `/willkommen/`), `mitwirkung_ruht.html` (403; H1 „Ihre Mitwirkungsrechte ruhen derzeit"; pausiert → Knopf „Beitrag jetzt überweisen (QR-Code)"; ausgeschlossen → Hinweis auf `didide@ddoe.at`), `nicht_stimmberechtigt.html` (403; H1 „Noch nicht stimmberechtigt"), `keine_ordnung.html` (503; H1 „Derzeit nicht möglich"), `gremien/kein_zugang.html` (403), `mitglieder/verwaltung_kein_zugang.html` (403), `token_ungueltig.html` (400).

---

# 3. Layout und Design-System (`verfahren/templates/verfahren/base.html`)

## 3.1 Header (`base.html:327-358`)
- Logo-Text `.marke`: „Parlament**Plattform**" (Serif 23 px, „Plattform" kursiv in `--goldsoft`) + `<small>` „Direkte Demokratie Österreich" (Uppercase, 10.5 px, Tracking .22em). Link → `/`.
- Mobile: `input#menue-schalter` (Checkbox, `aria-label="Menü"`) + `label.menue-knopf` (3 Balken, `aria-hidden`); ≤ 759 px wird `nav` ausgeblendet und per `:checked ~ nav` eingeblendet (Spalte, Animation `einfahren-oben .28s ease-out`, Balken → X-Transform, `:72-81`). Reines CSS.
- `nav` — Reihenfolge und Wortlaut (`:333-356`):
  1. **„Parlament"** (`/parlament/`)
  2. **„Mandatare"** (`/mandatare/`)
  3. **„Übersicht"** (`/uebersicht/`)
  4. **„Umsetzungsregister"** (`/umsetzung/`)
  5. **„Zukunftswerkstatt"** (`/zukunftswerkstatt/`)
  6. **„Antrag einbringen"** — `.nav-cta` (goldene Outline-Pille, Hover gefüllt), immer sichtbar, für Gäste Redirect zum Login
  7. angemeldet: **„Mein Gremium"** (nur `user.hat_gremienrolle`) · **„Beitrag"** (msgctxt `mitgliedsbeitrag`) · **„Verwaltung"** (nur `hat_adminrechte`; **nicht übersetzt**) · `span.wer` Anzeigename · Formular-Button **„Abmelden"**
  8. Gast: **„Anmelden"** · **„Mitglied werden"** (`.btn`, voll gold)
  9. Sprachumschalter: POST-Formular an `set_language` (hidden `next` = aktueller Pfad) mit Button **„EN"** bzw. **„DE"** (`.sprache`, Outline-Pille, `lang`-Attribut)
- Aktive Seite bekommt `class="an"` per Pfadvergleich (nur für die 5 Hauptpunkte); Hover-Pille `rgba(232,194,122,.12)`.
- Kein „Lebensbereiche"-Menüpunkt; keine Favicon-Deklaration; `<title>` „<Seite> · DDÖ".

## 3.2 Footer (`:368-404`)
- Spalte 1: Marke „Parlament**Plattform**"; Text „Direkte Demokratie Österreich — die Mitglieder entscheiden, die Mandatare setzen um, jede Auszählung ist unabhängig nachrechenbar."; kursiv „Wir sind das Werkzeug."
- Spalte 2 **„Plattform"**: „Parlament" · „Mandatare" · „Gremien" · „Mitglied werden" · „Lebensbereiche" (→ `/parlament/#feld-favoriten`) · „Die Plattform in Zahlen" · „Umsetzungsregister" · „Zukunftswerkstatt".
- Spalte 3 **„Offenheit"**: „Quellcode (AGPL)" (github.com/parlamentplattform/parlamentplattform) · „Register als JSON" (`/umsetzung.json`) · „Offene Parameter" (`/parameter/`) · „Internationale Partner" (`/partner/`) · „Satzungsentwurf" (ddoe.at/satzung/) · „ddoe.at".
- Fußzeile `.fuss-unten`: „Direkte Demokratie Österreich" · „Ohne Skriptzwang, ohne Tracking, ohne verdeckte Reihung."

## 3.3 Tokens (`:9-36`)
- Farben hell (`:9-22`): `--ink #14232E` (Text) · `--matt #5E6F7A` (Nebentext) · `--deep #0E4C5C` · `--linkfarbe #0E4C5C` · `--gold #D9A441` · `--goldsoft #E8C27A` · `--goldtief #8A6415` · `--paper #F4F1E9` (Body) · `--karton #FFFFFF` (Karten/Felder) · `--line rgba(20,35,46,.14)` · `--nacht1 #0A1722` · `--nacht2 #0E2230` · `--nacht3 #10394A` (Header/Held-Verlauf) · `--glanz rgba(255,255,255,.55)`.
- Status: `--ok-rand #2E6B4F` / `--ok-grund #EFF6F1` / `--ok-text #1E4736`; `--info-grund #EDF3F5` / `--info-text #0E4C5C`; `--warn-rand #8C2B2B` / `--warn-grund #F9EFEE` / `--warn-text #6E2222`; `--hinweis-grund #FBF6EA`; `--stern-aus #b9c2c7`.
- Schatten: `--schatten 0 1px 2px rgba(14,34,48,.06), 0 6px 18px rgba(14,34,48,.05)`; `--schatten-hoch 0 2px 4px …,0 14px 34px rgba(14,34,48,.12)`.
- Farben dunkel (`:23-36`): `--ink #E9E4D8`, `--matt #98A6AE`, `--deep #8CC0CF`, `--linkfarbe #9BCBD8`, `--goldtief #E0B866`, `--paper #0C151E`, `--karton #132029`, `--line rgba(233,228,216,.16)`, Statusfarben entsprechend dunkel, `--glanz rgba(255,255,255,.04)`.
- Schriften: `--serif: Georgia,'Times New Roman',serif` (h1–h3 weight 400 mit `text-wrap:balance`, Marke, Zahlen im Zahlenband, Initialen-Platzhalter); Body `system-ui,-apple-system,'Segoe UI',Roboto,sans-serif` 16 px / Zeilenhöhe 1.65; `--sans: -apple-system,'SF Pro Display',BlinkMacSystemFont,'Segoe UI Variable Display','Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif` — **nur** für `.fknoten` (Fächer-Knoten, weight 600, letter-spacing −.02em) und die Texte des Flussdiagramm-SVG (`index.html:99`).
- Radien: 999 px Pillen (Buttons, Chips, Badges); `.karte` 16, `.feld` 18, `.held` 20, `.kachel` 12, Inputs 10, `.bereich` 16. Größen: h1 `clamp(28px,4.5vw,38px)`, h2 24 px, `.feld-kopf h2` 19 px, `.bk` 11.5 px Uppercase Tracking .16em, `.badge` 12 px Uppercase.
- **Dark Mode**: nur `@media (prefers-color-scheme: dark)` (`:23-36`, plus Einzelregeln `:103,:224,:230,:250`), `<meta name="color-scheme" content="light dark">`; kein manueller Schalter. Hart kodiert und nicht umschaltend: `vollzug_stil`-Badges (`templatetags/phasen.py:39-45`), Badges in `uebersicht.html:39-40`, `verwaltung_mitglied.html:8-11`, SVG-Ikonen (`#fff`, `#0E2230`) in `mitgliedschaft.html`/`einfuehrung.html`, Captcha-SVG, QR-Kasten.
- **Breakpoints**: 980 px (Parlament einspaltig, Felder `height:auto;max-height:72vh`, Kacheln einspaltig), 900 px (`.hf-grid`, `.fenster-grid` einspaltig), 759 px (Mobilmenü), 700 px (Footer einspaltig, Tabellen als Karten mit `data-label`), 640 px (Fächer-Schriften 20/18/16/15), 620 px (Zahlenband einspaltig).

## 3.4 Parlament-Grid / Kacheln / Feldkopf (`:135-183`)
- `main.breit{max-width:1500px}` (`:136`); `.parlament{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:6px}` (`:137`).
- `.feld{display:flex;flex-direction:column;height:calc(50vh − 66px);min-height:360px;overflow:hidden;position:relative;border-radius:18px;box-shadow:var(--schatten)}` (`:138`) → **zwei Reihen à (50vh − 66 px) + 16 px Lücke = 100vh − 116 px**; zusammen mit Header (~80 px) und `main` Padding-Top 36 px füllt das Raster ungefähr die Viewport-Höhe. Der Body bleibt scrollbar (Footer; `main` Padding-Bottom 90 px).
- Innen: `.feld-kopf` (flex, Padding 14/18/10, Border unten, H2 19 px, `.bk` rechts mit `margin-left:auto`, `:155-157`); `.feld-korpus{overflow-y:auto;flex:1;scrollbar-width:thin;padding:6px 18px 26px}` (`:158`); `.feld-fuss` (Border oben, 13 px, `:160`). `.feld::after{content:"";position:absolute}` (`:159`) ist ein leerer Stummel — **kein „Mehr vorhanden"-Hinweis**.
- Kacheln: `.kacheln` 2 Spalten (`:167`), `.kacheln.dreier` 3 Spalten (`:168`); `.kachel` (Border, Radius 12, Paper-Hintergrund, Flex-Spalte, `min-width:0`, `:169`); `.balken` 4 px hoch mit `b` in Gold (`:175-176`); `.stimmreihe` Knöpfe 12.5 px, `.gewaehlt` Gold-Outline (`:177-179`).
- Filter: `.filter-leiste` (Chips 12.5 px, `.chip.an` Gold, `:139-143`), `.regler-klappe summary` ohne Marker (`:144-145`), `.regler-feld` siehe 2.1 (`:146`), `.regler` 13 px mit `output` in Goldtief, `input[type=range]{accent-color:var(--gold)}` (`:147-150`).

## 3.5 Keyframes/Transitions (vollständig, `:269-318`)
| Name | Definition | Einsatz |
|---|---|---|
| `auftauchen` | opacity 0 + translateY(22px) → none | `main .feld/.karte/.held/.wk/.schritt/.grundsatz/.fluss-rahmen` .6s `cubic-bezier(.22,.8,.3,1)` both, Staffelung .06/.12/.18/.24/.3 s (bis 6. Kind); `.anstoss-bestaetigung` .4s; `.anstoss-karte` .32s |
| `hineingleiten` | opacity 0 + translateY(12px) scale(.955) → none | `.faecher` .42s cubic-bezier(.22,.8,.3,1) |
| `einfahren-oben` | opacity 0 + translateY(−8px) → none | `.filter-leiste` .4s ease-out; mobiles `nav` .28s ease-out |
| `einfahren-rechts` | opacity 0 + translateX(18px) → none | `.regler-feld` .32s cubic-bezier(.22,.8,.3,1) |
| `wachsen` | scaleX 0 → 1 (origin left) | `.balken b` .8s Delay .25s + `transition:width .4s` |
| `blase-auf` | 0 % opacity 0/translateY(8px) scale(.96) → 12 % sichtbar → 88 % → 100 % opacity 0/translateY(−4px) | **nirgends verwendet** |
Transitions: `.kachel` transform/box-shadow/border-color .18s (Hover −3 px, Gold-Kante), `.zeile` background .15s (Hover 5 % Gold), `.btn/.btn-linie/.btn-hell/.btn-still/.chip` transform/box-shadow/background/border .16s (Hover −1 px, Active scale .965), `.stern` transform .18s `cubic-bezier(.3,1.6,.5,1)` (Hover scale 1.22, Active .9), `nav a` .16s, Inputs border/box-shadow .15s, `.menue-knopf span` .2s, `.kat-grid>*` .15s (Hover −2 px), `.wk` .15s, `.fknoten a` color .15s, `html{scroll-behavior:smooth}`. **View Transitions**: `@view-transition{navigation:auto}`, `::view-transition-old(root)` .16s, `::view-transition-new(root)` .24s (`:303-305`) — cross-document, richtungsunabhängig. **prefers-reduced-motion**: zwei Blöcke — `:306-309` setzt alle Dauern auf .001s und View-Transitions auf none; `:318` setzt zusätzlich `transition:none!important;animation:none!important` und `scroll-behavior:auto` (doppelt). Print (`:319`): Header/Footer/Nav/Anstoß ausgeblendet.

## 3.6 JavaScript
- `verfahren/static/verfahren/js/htmx.min.js` — **htmx 2.0.10** (`version:"2.0.10"` im Dateikopf), `defer`; `<meta name="htmx-config" content='{"defaultSwapStyle":"outerHTML"}'>`; `<body hx-headers='{"X-CSRFToken": …}'>` (`base.html:321-325`).
- `alpine.min.js` — **Alpine.js 3.17.1**, `defer` geladen, **in keinem Template verwendet** (0 × `x-data`/`x-show`/`x-transition`).
- Inline-JS: `oninput` an den Range-Reglern (Live-Wert in `<output>`, `parlament.html:39`); `onclick` an den beiden Anstoß-X (`_widget.html:6,12`); `hx-on::after-request` Formular-Reset; `_meldung.html` Inline-`<script>` (schließt `<details>`, zeigt Blase). Kein weiteres eigenes JS, kein `<noscript>`.

## 3.7 Anstoß-Knopf
`.anstoss-ecke{position:fixed;right:18px;bottom:18px;z-index:80}` (`:257`); Summary = goldene Pille „Anstoß" mit Sprechblasen-Icon (Schatten `0 8px 22px rgba(0,0,0,.22)`); Karte öffnet nach oben (`bottom:54px`, Breite ≤ 340 px, Schatten `0 18px 44px`); Bestätigungsblase darüber (`animation:auftauchen .4s`). Überlappt als Fixed-Element die rechte untere Ecke jeder Seite — im Parlament das Feld „Meine Region" (Direktabstimmungs-Knöpfe/Scrollbalken) sowie den Footer; liegt über dem Regler-Overlay (`z-index 30`). Im Druck ausgeblendet.

---

# 4. Management-Commands, Betrieb, CI

- **`demo_seed`** (`verfahren/management/commands/demo_seed.py`) — läuft bei **jedem Render-Deploy** (`render.yaml:30`):
  - ruft `gemeinden_laden`; legt Verfahrensordnung `sachantrag-standard` v2 aktiv an (Schwelle 3, Unterstützungsfrist 60 Tage, Beratung 21, Abstimmung 28, Mindestbeteiligung 0.05, `ja_nein`, Sperre 6 Monate; `:36-53`);
  - 5 Mitglieder `demo1..demo5` (`demo<i>@example.org`, Beitritt vor 200 Tagen, Identität geprüft, Pseudonym „Mitglied i", Gemeinde St. Marienkirchen an der Polsenz/OÖ, unbenutzbares Passwort, **kein Admin**; `:54-71`);
  - nur bei leerer Antragstabelle (`:73-196`): a1 Unterstützung („Sitzungsprotokolle binnen 48 Stunden veröffentlichen"), a2 Beratung („Testlauf: monatlicher öffentlicher Entwicklungsbericht", 4 Unterstützungen, 2 Kommentare), a3 angenommen („Abgeschlossenes Beispiel: Namenskonvention des Prototyps" — Zeitraffer über `phase_beginn`, Stimmen ja/ja/ja/nein/enthaltung, Vollzug in_umsetzung → umgesetzt), a4 Abstimmung hervorgehoben („Jede Ratssitzung als Livestream mit Archiv", Begründung „Betrifft alle Gremien dauerhaft, hat aber bisher wenig Beteiligung. Beschluss IR-2026-03 vom 12.08.2026.", 2 Stimmen), a5 regional Gemeinde („Photovoltaik auf dem Dach des Gemeindeamts"); Favoriten a4/a2 für demo1; `kategorien_laden` + automatische Zuordnung aller Anträge; Abos `energie`, `umwelt-klima` für demo1;
  - eigener Wächter (`:200-230`): Mandats-Kandidatur „Testlauf: Listenreihung Gemeinderat St. Marienkirchen an der Polsenz" (Gemeinde, 2 Bewerbungen, 3 Unterstützungen, per Zeitraffer in Abstimmung, 3 Zustimmungen);
  - eigener Wächter (`:236-268`): Rollen demo2/demo3 → Gruppe 1, demo4 → Gruppe 2, demo1 → Koordinationsrat (alle MV-bestätigt, Ende = Registerdauer) + Entwurfsfenster am Beratungsantrag mit Fassung 1 und einem Beitrag;
  - Parameter-Erstbestand sicherstellen (`:272-276`).
- **`kategorien_laden [--datei]`**: höchste `policies/kategorien-v*.yaml`; `update_or_create` per Slug (Name, Eltern, Beschreibung, EuroVoc „; "-verkettet, Schlagworte, Reihenfolge = Besuchsnummer, aktiv), nicht mehr geführte → `aktiv=False`.
- **`gemeinden_laden`**: `daten/gemeinden.csv` (`;`, Kommentarzeilen `#`), `update_or_create` per Kennziffer.
- **`beitraege_abrufen`**: `bank.abgleich_ausfuehren(erzwungen=True)` (für einen späteren Cron).
- **`render.yaml`**: Web-Service `parlamentplattform` (Docker, Frankfurt, Plan starter, Healthcheck `/gesund/`), `dockerCommand`: `migrate` → `kategorien_laden` → `demo_seed` → `collectstatic` → `gunicorn config.wsgi --workers 2 --timeout 60`; Env: `DDOE_DEBUG=0`, `DDOE_SECRET_KEY` generiert, Hosts/CSRF `parlamentplattform.onrender.com,parlament.ddoe.at`, `DDOE_STATIK=whitenoise`, `DDOE_UEBERGANGSREGEL=1`, `DDOE_MAIL_ABSENDER=plattform@ddoe.at`, SMTP-Variablen `sync:false`, Postgres aus DB `plattform-db` (basic-256mb, PG 16).
- **`Dockerfile`**: `python:3.12-slim`, `pip install ".[postgres,prod]"`, CMD `migrate && runserver 0.0.0.0:8000` (Entwicklungsserver; Render überschreibt per `dockerCommand`).
- **`docker-compose.yml`**: `db` postgres:16-alpine (`plattform/plattform/nur-entwicklung`, Healthcheck) + `web` (build ., Port 8000, `DDOE_DEBUG=1`).
- **`Makefile`**: `dev` (venv + `pip install -e ".[dev]"`), `test` (`coverage run -m pytest -q` + report), `lint` (ruff), `run` (migrate + runserver), `seed` (demo_seed), `check` (`manage.py check --deploy`).
- **CI `.github/workflows/ci.yml`**: Job `pruefen` (Python 3.12, `pip install -e ".[dev]"`, `ruff check .`, `coverage run -m pytest -q` + `coverage report` mit `fail_under=90` für `plattform_core` (`pyproject.toml:41-47`), `manage.py check`); Job `ausrollen` (nur Push auf main, nach `pruefen`, `curl` auf Secret `RENDER_DEPLOY_HOOK`, sonst übersprungen).
- **`pyproject.toml`**: Version **0.1.0** (Projekt selbst nennt sich 0.32.0 nur im CHANGELOG); Abhängigkeiten Django ≥5.0,<5.3, PyYAML, segno, requests; dev: pytest, pytest-django, hypothesis, ruff, coverage; ruff line-length 110, Regeln E/F/W/I/B/UP/DJ.
- **`docs/BETRIEB-RENDER.md`** (Kernaussagen): Produktion `https://parlament.ddoe.at` (Ausweich `parlamentplattform.onrender.com`), Starter-Instanz + basic-256mb ≈ 13 $/Monat; Free-Instanzen können kein SMTP; Auto-Deploy nur über CI-Hook; Start-Reihenfolge wie oben; Env-Tabelle; kein Django-Admin/`createsuperuser` — Verwaltung über `/verwaltung/` mit `DDOE_FIX_ADMIN`; Backups (Render-Snapshots + monatlicher `pg_dump`); Phasenübergänge lazy beim Seitenaufruf, optionaler Cron-Einzeiler; Datenschutz-Einordnung (US-Anbieter, Art 9 DSGVO, Umzug auf EU-Anbieter vorgesehen).
- **i18n**: `locale/en/LC_MESSAGES/django.po` (POT-Datum 2026-09-02): **758 msgid-Einträge** (759 `msgid`-Zeilen inkl. Header), 8 Pluralformen, 1 `msgctxt`; **0 fuzzy, 0 leere msgstr**, keine obsoleten Einträge; `django.mo` kompiliert. Nicht übersetzbar: alle 8 Verwaltungs-Templates (ohne `{% load i18n %}`), Nav-Punkt „Verwaltung", Verwaltungs-Messages, Mail-Texte des Beitragsabgleichs, der KI-Auftrag.

---

# 5. Verifikations-Checkliste

1. **Bildschirmfüllendes 2×2-Raster?** — **JA (näherungsweise).** `.parlament` 2 Spalten (`base.html:137`), `.feld{height:calc(50vh - 66px);min-height:360px;overflow:hidden}` (`:138`); Korpus scrollt intern (`:158`). Zwei Reihen = 100vh − 116 px; darüber Header + 36 px Main-Padding; die Seite bleibt insgesamt scrollbar (Footer). ≤ 980 px: einspaltig, `height:auto;max-height:72vh` (`:183`).
2. **Fächer zeigt 5 Ebenen gleichzeitig?** — **TEILWEISE.** Im Modus `boden` (Tiefe 1–2) 3 Ebenen (Anker + Kinder + Enkel ≤ 12; `faecher.py:67-71,111-135`); im Modus `mitte` (Tiefe ≥ 3) zusätzlich unten der Rückweg mit max. 2 Vorfahren + Wurzel → 5 (Tiefe 3) bzw. 6 (Tiefe 4) Ebenen, ab Tiefe 5 mit Lücke (`:84-89,137-145`). Schriftgrößen: Anker 24, Kinder 22, Enkel 20, Weg 22/20, Wurzel 18 (`:111,123,133,140,144`; CSS `base.html:204-207`, mobil 20/18/16/15 `:212`). An der realen Wurzel: 17 Knoten (1+4+12).
3. **Hinein-Slide-Animation beim Fächer?** — **JA:** `.faecher{animation:hineingleiten .42s cubic-bezier(.22,.8,.3,1) both}` (`base.html:281`), Keyframe `hineingleiten` = Einblenden + 12 px von unten + Skalierung .955 → 1 (`:270`). Wird bei jedem htmx-Tausch des Feldes neu abgespielt; keine richtungsabhängige Bewegung, kein Auffächern der Knoten.
4. **Einfahrbare Profil-Leiste mit Pfeil?** — **NEIN.** Feste Chip-Leiste `.filter-leiste` (Neutral + Profile + `<details>` „⚙ Regler"), die beim Laden von oben einfährt (`einfahren-oben .4s`, `:283`); kein Pfeil, kein Slide-Mechanismus (`parlament.html:20-56`).
5. **Regler:** **8**, wörtlich (`views.py:31-40`): „Mehr, wo ich schon abgestimmt habe", „Mehr, wo ich unterstützt habe", „Entdeckungen außerhalb meiner Favoriten", „Mehr Unterstützungsphase", „Mehr laufende Abstimmungen", „Neues zuerst", „Bald ablaufende zuerst", „Knapp vor der Schwelle zuerst". **„wofür/wogegen ich gestimmt habe": NEIN** — `gestimmt` misst nur die Kategorien-Überschneidung mit Anträgen, bei denen ein eigener Stimmregister-Eintrag existiert, richtungsunabhängig (`views.py:62-64,80`). „chronologisch" **JA** („Neues zuerst"), „bald ablaufend" **JA**, „knapp vor der Schwelle" **JA**.
6. **Halbtransparentes Overlay rechts?** — **JA.** `<details class="regler-klappe">` (`parlament.html:31-55`) + `.regler-feld{position:absolute;right:8px;top:46px;bottom:8px;z-index:30;width:min(330px,calc(100% - 16px));background:color-mix(in srgb,var(--karton) 90%,transparent);backdrop-filter:blur(6px)}` (`base.html:146`), Animation `einfahren-rechts` (`:284`). Kein Alpine.
7. **Wichtige Abstimmungen:** Kacheln im **2-Spalten-Grid** (`.kacheln`, `base.html:167`), **kein Limit** im Parlament (`views.py:266`), Startseite max. 3 (`:191`). Hervorhebung = Feld `Antrag.hervorgehoben` + Begründung (`verfahren/models.py:203-212`), laut Kommentar Integritätsrat — **es existiert kein UI/Endpoint zum Setzen** (nur `demo_seed.py:154-159`; Django-Admin nicht geroutet). Kachel zeigt „p % der Stimmberechtigten haben abgestimmt · Tendenz verdeckt bis Fristende" (`_kachel.html:17-18`) — **Beteiligung ja, „wofür" nein**.
8. **Meine Region 3×3?** — **TEILWEISE.** 3 Zeilen Gemeinde/Bezirk/Land immer (`views.py:283-295`), je Zeile `.kacheln.dreier` mit 3 Spalten (`base.html:168`), aber unbegrenzte Kachelzahl je Zeile (Zeilenumbruch). **Direkt abstimmbar: JA** (Ja/Nein/Enthaltung-Formular in der Kachel, hx-post mit Rückkehr ins Feld; `_kachel.html:25-36`, `views_aktionen.py:288-292`).
9. **„Mehr vorhanden"-Scroll-Hinweis?** — **NEIN.** Nur `.feld-korpus{overflow-y:auto}`; `.feld::after` ist ein leerer Stummel (`base.html:159`); `.feld-fuss` nur für „← Zurück zum Fächer" (`parlament.html:121-124`).
10. **Antragsseite in 3 Zonen?** — **NEIN.** Einspaltige Abfolge (`antrag.html`): Kopf → Wortlaut/Begründung → phasenabhängige Karte → Ergebnis → Umsetzung → Entwurfsschleife → Beratung → Regeln. Keine KI-Einschätzung, keine Grafiken/Animationen (nur `.karte` `auftauchen`), kein Chat.
11. **Chatsystem:** Kommentare unterhalb **JA** (H2 „Beratung (n)", chronologisch, Formular unten, `antrag.html:175-195`); Scrollposition merken **NEIN**; Ausklapp-Panel mit 3 Spalten **NEIN**; Chat-Räumung bei Hochstufung **NEIN** (Kommentare bleiben, Formular nur in Unterstützung/Beratung, `views_aktionen.py:257`); Archiv-Registerkarte **NEIN**; Export **NEIN** (`export.json` enthält nur Stimmen/Bewerbungen).
12. **Abstimmungs-Chat mit Emoji-Zustimmung/Reihung/„Passt alles" > 50 %?** — **NEIN.** Stattdessen `UnterstuetzerVotum` (annehmen / zurückgeben mit Pflicht-Wunsch, offen mit Namen, `_schleife.html:27-33`, `gremien/views.py:282-311`); Auswertung nach 14 Tagen oder sobald alle Unterstützer gestimmt haben: Rückgaben > Annahmen ∧ Runde < 3 → Rückgabe, sonst (auch bei Gleichstand oder Stille) Endabstimmung (`gremien/models.py:288-310`). Keine Reihung nach Engagement, keine Reaktionen.
13. **Fristen:** Unterstützung 60 Tage → `unterstuetzung_frist_tage` in VO v2 (`demo_seed.py:44`, `migrations/0008:12`; YAML v1 nennt 14, `grundordnung-v1.yaml:17`). Beratung 21 Tage → `beratung_tage` (Policy, Minimum `policy.py:16`); ein **eigener** „Expertenrat 21 Tage Erstvorschlag"-Parameter existiert **nicht** — die Beratungsfrist ist das Fenster; ein nie eingereichter Entwurf hält nichts auf (`verfahren/models.py:272-281`, `gremien/models.py:168-180`). Unterstützer 14 Tage → `REVIEW_TAGE=14` / Register `gremien-review-tage` (`gremien/models.py:35`, `parameter/models.py:47-54`). Überarbeitung 14 Tage → `UEBERARBEITUNG_TAGE` / `gremien-ueberarbeitung-tage` (`:36`, `:55-62`). Endabstimmung 28 Tage → `abstimmung_tage` VO v2 (`demo_seed.py:46`; YAML v1: 7). Höchstrunden 3 → `gremien-hoechstrunden`. Die Fristen 60/21/28 stehen **nicht** im Parameterregister, sondern nur im Policy-Snapshot; im Startseiten-Diagramm sind sie hart eingetragen (`index.html:118,122,126`).
14. **Rollen:** existieren als `Gremium`: `expertenrat1`, `expertenrat2`, `koordinationsrat`, `integritaetsrat` (`gremien/models.py:49-53`). **Mandatar und Internationaler Partner: NEIN** (Mandat ist ein eigenes Modell ohne Rechte; Partner nur angekündigt, `partner.html:48-49`). Zuweisung über Admin-UI `/verwaltung/rollen/` (berufen mit Enddatum, MV-Bestätigung, Beenden mit Grund; `gremien/views.py:317-391`). Automatisch: nur `demo_seed` und die Massenbeendigung der G1-Rollen bei KoRat-Stattgabe (`:520-524`). **Integritätsrat hat keinerlei Funktion/Bereich** (einzige Verwendung `gremien/models.py:53`; `mein` leitet ihn zur öffentlichen Übersicht, `gremien/views.py:92-98`).
15. **Expertenrat-1-Bereich:** **JA, vollständig** — roher Antrag links (`fenster.html:17-23`), Entwurfsfenster öffnen (`gremien/views.py:171-187`), Fassungen append-only, interne Beiträge, interne Einreich-Abstimmung (Mehrheit ≥ ½ der aktiven Rollen, `gremien/models.py:144-159`), Einreichen → (Vollzugsbezug: Gruppe 2 →) Vorlage an Unterstützer (`_schleife.html`) → Votum zurück/weiter (14 Tage, max. 3 Runden) → Endabstimmung als neue Antragsfassung (`gremien/models.py:233-276`). Zusätzlich optionale KI-Einschätzung.
16. **Expertenrat-2-Bereich:** **JA** — `/gremien/pruefung/` mit „Validieren" / „Begründet zurückgeben" / „Austausch beim KoRat beantragen", Begründung Pflicht und öffentlich (`pruefung.html:18-24`, `views.py:430-462`).
17. **Koordinationsrat:** eigene Oberfläche **JA** (`/gremien/koordination/`); entscheidet Austauschanträge (Stattgeben beendet alle G1-Rollen); Rollenübersicht. Hinweise der Zukunftswerkstatt: **Platzhalter** („Zukunftswerkstatt-Posteingang", `koordination.html:45-50`). Interne Abstimmung über Umsetzung: **NEIN** — ein Mitglied entscheidet allein (`views.py:499-543`).
18. **KI:**
    - Ähnlichkeitsprüfung beim Einbringen: **JA, lexikalisch** — Trigramm-Jaccard, Schwelle 0.18, Top 3, nur gegen offene Anträge (Titel + Wortlaut) (`similarity.py:48-73`, `views_aktionen.py:170-197`); kein Embedding, kein Modell.
    - Wahl „bestehenden unterstützen oder eigenen stellen": **JA** — Trefferliste mit Beteiligung, Knöpfe „Trotzdem einbringen" / „Lieber einen bestehenden Antrag unterstützen" (→ Parlament) (`einbringen.html:11-41`).
    - Rechtsfolgen-Einschätzung (Gesetze/Judikatur/Exekutive/Beamte), Aufwand/Dauer, Ausschreibungsprüfung: **NEIN** — nur Prosa („In Entwicklung", `einbringen.html:46-48`; Zukunftswerkstatt-Seite; Mitgliedschaftsseite).
    - `ki`-Modul konkret: `MistralAnbieter` (Chat Completions über `urllib`, `temperature 0.2`, `max_tokens 900`, Timeout 45 s), `AttrappenAnbieter` (Tests), `KILauf`-Archiv (append-only, auch Fehlschläge), Monats-Tokenbudget aus dem Register, genau ein Zweck „einschaetzung"; einzige Nutzung: Knopf „KI-Einschätzung einholen" im Entwurfsfenster der Gruppe 1.
    - **Prompt wörtlich** (`gremien/views.py:42-49`): „Du bist Fach-Assistent des Expertenrats der ParlamentPlattform (Direkte Demokratie Österreich). Prüfe den folgenden Antragsentwurf: Fasse ihn in drei bis fünf Sätzen zusammen, benenne Unklarheiten im Wortlaut, offene Vollzugs- oder Kostenfragen und mache konkrete Formulierungsvorschläge. Liegen Wünsche der Unterstützer bei, prüfe, ob die Fassung sie aufgreift. Du machst Vorschläge — jede Entscheidung treffen Menschen. Antworte auf Deutsch, nüchtern und knapp, in Fließtext ohne Listen." Eingabe-Aufbau `:217-226` (Titel, Entwurfsfassung N (Runde R), Begründung, Wünsche der Vorrunde).
    - Schlagwort-Klassifikation (`klassifikation.py`) ist deterministisch — keine KI.
19. **Mitgliedschaftsseite:** Funktionen & Rechte plakativ **JA** (6 Ikonen-Karten „Was Sie als Mitglied können", `mitgliedschaft.html:18-78`) + Detail **JA** (4 Bereiche `:123-153`); Erklärung Zukunftswerkstatt **JA** (Karte `:155-163`, verweist auf StaatsSimulation); Grafiken **JA** (6 Inline-SVG-Ikonen); Flowchart: **TEILWEISE** — vertikaler CSS-„Fluss" mit 6 nummerierten Stationen (`:80-121`), kein Diagramm-SVG (das liegt auf `/`).
20. **Menü:** wörtlich „Parlament · Mandatare · Übersicht · Umsetzungsregister · Zukunftswerkstatt · Antrag einbringen · [Mein Gremium] · Beitrag · [Verwaltung] · <Name> · Abmelden | Anmelden · Mitglied werden · EN/DE" (`base.html:333-356`). „Lebensbereiche" **ist aus dem Menü** (nur Footer-Link `:382`). Heißt **„Umsetzungsregister"** — JA (`:336`). „Antrag einbringen" prominent — JA (`.nav-cta`, Outline-Gold, `:338`, `:68-69`). „Parlament" als Menüpunkt — JA (`:333`, Test `test_nav_heisst_parlament`).
21. **Mandatare-Seite:** Foto **JA** (BinaryField, JPEG/PNG/WebP ≤ 800 kB, `mandatare/models.py:23-39,65-66`), Aufgaben mit Fristen und Status **JA**, Entscheidungsprozesse = Aufgaben mit Beschreibung und verknüpftem Antrag **JA** (`detail.html:26-43`). Mandatar-Rolle mit Instant-Reports: **NEIN** (Pflege nur durch Verwaltung; M2 nur im Docstring `mandatare/models.py:7-9`).
22. **Kandidatur als Antrag / Zustimmungswahl / Listenreihung:** **JA / JA / JA** — `Antragsart.MANDAT`, `Bewerbung`, `BewerbungsZustimmung` (mehrfach zustimmbar, Umschalter, pseudonym), `personenwahl_auszaehlen` reiht nach Zustimmungen, Ergebnis-Tabelle „Platz/Bewerbung/Zustimmungen" (`antrag.html:96-111`, `tally.py:160-231`). Kein automatisches Anlegen eines `Mandat` aus dem Wahlergebnis (`Mandat.kandidatur` wird nie gesetzt).
23. **Partner-Seite:** Strategie **JA**, Fahrplan (3 Stufen) **JA**, Kontakt-Button **JA** (`mailto:`), Konto-Anlage/Bestätigung/Rolle „Internationaler Partner" **NEIN** (`partner.html:43-50`).
24. **Anstoß-Widget:** auf jeder Seite **JA** (`base.html:367`); Speicherung: Tabelle `anstoss_anstoss` (`anstoss/models.py:20-43`); Verwaltung + CSV/JSON-Export **JA** (`/verwaltung/anstoesse/…`); Auto-Schließen nach Senden **JA** (htmx: `_meldung.html`-Script; ohne JS: Redirect mit `?anstoss=danke` lässt `<details>` geschlossen); **X JA** (Karte und Bestätigungsblase, `_widget.html:5-6,11-12`); Bearbeitungs-Vermerk nicht per UI setzbar.
25. **Namen:** „StaatsSimulation" — **JA, 4 Stellen**: `mitgliedschaft.html:158`, `zukunftswerkstatt.html:10,71`, `einbringen.html:48` (Route `/staatssimulation/` nur Redirect). „Zukunftswerkstatt" — Nav, Footer, Startseite, Mitgliedschaft, Koordination, eigene Seite. „WeicherFilter" — Feldtitel `parlament.html:16` und Startseiten-Karte `index.html:205`. „Alpha" — `index.html:244`, `gremien/uebersicht.html:27`, `koordination.html:42`. „Prototyp" — `uebersicht.html:100` und Demo-Antrag a3 (`demo_seed.py:106-107`).
26. **Schriften:** Überschriften h1–h3 **Serif** (`Georgia,'Times New Roman',serif`, weight 400, `base.html:19,40`); Body System-Sans (`:39`); **„moderne Sans" `--sans` ausschließlich für Fächer-Knoten (`:199`) und die SVG-Texte des Flussdiagramms (`index.html:99`)** — JA.
27. **Ohne JavaScript:** Alles bleibt bedienbar (jede htmx-Aktion ist ein normales `<form method=post>` / `<a href>` mit Redirect + Anker `#feld-…`; Mobilmenü per Checkbox; Sprachumschalter per POST; Anstoß per `<details>` + Redirect-Parameter). Es fehlen nur: Teil-Austausch der Felder (stattdessen Vollreload), Live-Zahl an den Range-Reglern (`oninput`), Formular-Reset/Auto-Close des Anstoß-Panels via Script (Server-Fallback vorhanden), View-Transitions bleiben (reines CSS). Kein `<noscript>`; Alpine ungenutzt.
28. **Skeleton-Zustände: NEIN. Zahlen-Hochzählen: NEIN. Gerichtete Übergänge je Richtung: NEIN** — nur statische Keyframes und ein richtungsloser Root-Crossfade (`base.html:303-305`).
29. **Barrierefreiheit:** Skip-Link „Zum Inhalt springen" (`:326`); `aria-label` am Menü-Schalter, Suchfeld, Anstoß-Summary/-X, SVG-Ikonen/Diagrammen (`role="img"`), `aria-live="polite"` am Anstoß-Echo, `role="status"` an der Blase, `role="search"`; Fokusring 2 px Gold/Ink-Mix mit Offset 2 px (`:299`, überschreibt `:44`); Kontrast (berechnet): Ink #14232E auf Paper #F4F1E9 14.2:1, Matt #5E6F7A auf Paper 4.6:1 (AA), Deep #0E4C5C auf Paper 8.4:1, Goldtief #8A6415 auf Paper 4.75:1, Ink auf Gold-Button 7.1:1, Goldsoft auf Nacht 9.6:1; Diagramm-Gold #D9A441 auf Weiß nur 2.25:1 und wird darum immer beschriftet (`diagramme.py:9-10`). Barrieren: Captcha nur als Bild (Verweis auf E-Mail, `botschutz.py:50-52`), Sterne nur per Unicode-Zeichen mit `title` (kein `aria-pressed`), Regler ohne `aria-valuetext`, Fächer-Fäden `aria-hidden`.
30. **Tote/unbenutzte Elemente:** `/kategorien/`, `/kategorien/<slug>/` (reine Redirects), `/staatssimulation/` (Redirect); `keine_ordnung.html` **wird benutzt** (503 ohne aktive VO, `views_aktionen.py:159-160`); `verfahren/admin.py` + `mitglieder/admin.py` registrieren Admin-Klassen ohne Route; `alpine.min.js` ungenutzt; CSS-Leichen `.mini-kacheln/.mini-kachel` (`base.html:180-182`), `.brotkrume` (`:218-219`), `@keyframes blase-auf` (`:274`), `.feld::after` (`:159`); `policies/grundordnung-v1.yaml` und `kategorien-v1.yaml` werden von keinem Code gelesen; `Phase.ZURUECKGEWIESEN/ZURUECKGEZOGEN`, `Antrag.zurueckweisung_begruendung`, `Gremium.INTEGRITAETSRAT`, `Mandat.kandidatur`, `Anstoss.vermerk`-Eingabe, `Gegenstand.MANDATSNOMINIERUNG/SATZUNGSAENDERUNG/AUFLOESUNG`, `Identitaetsstufe.PRAESENZ/EID` (nur per Verwaltungs-Select setzbar) — ohne Schreib-/Nutzpfad; `StimmRegister.pruefcode` wird nirgends angezeigt.

---

# 6. Auffälligkeiten / Inkonsistenzen im Code

1. **Kein UI für „Hervorheben" und „Zurückweisen"**: `Antrag.hervorgehoben` und `zurueckweisung_begruendung` (Integritätsrat laut § 5) sind nur über `demo_seed` bzw. gar nicht setzbar; der Django-Admin ist absichtlich nicht geroutet, `admin.py`-Registrierungen sind tot.
2. **Rolle Integritätsrat ohne Funktion**: `hat_gremienrolle` blendet „Mein Gremium" ein, `mein` führt ins Leere (`gremien/views.py:92-98`).
3. **Fristen doppelt und verstreut**: Konstanten `REVIEW_TAGE/UEBERARBEITUNG_TAGE/HOECHSTRUNDEN/ROLLEN_DAUER_TAGE` (`gremien/models.py:35-38`) ≙ `ERSTBESTAND` (`parameter/models.py`), `MONATSTOKENS_STANDARD` ≙ `DDOE_KI_MONATSTOKENS`-Default; Verfahrensfristen 60/21/28 stehen in VO-Snapshot, Migration, `demo_seed` und hart im Startseiten-SVG, nicht im Parameterregister; `grundordnung-v1.yaml` (14/21/7) widerspricht der aktiven VO v2 und wird nicht importiert.
4. **Harte Konstanten, die Parameter sein sollten**: Ähnlichkeitsschwelle 0.18/Limit 3 (`similarity.py:51-52`), Klassifikations-Limit 3, `ENKEL_HOECHSTZAHL=12`, `FilterProfil.HOECHSTZAHL=5`, 60-Tage-Horizont `ablaufend` (`views.py:77,86`), Anstoß-Drossel 60 s/20 pro Tag, Bot-Drossel 5/10 pro Stunde, Mindestzeit 5 s, `ABRUFE_PRO_TAG=4`, Consent 180 Tage / `max_historical_days 90` / `ABRUF_TAGE 14` (`bank.py`), IBAN + Richtwert 30 € (`mitglieder/views.py:182-183`), KI `temperature 0.2`/`max_tokens 900`/Timeout 45 s, Foto 800 kB, Upload 5 MB, Listen-Limits 200/300/20/15/10/8.
5. **Widerspruch „Tendenz verdeckt"**: Kacheln und Antragsseite verbergen Zwischenstände, aber `/uebersicht/` zeigt für laufende Abstimmungen Ja/Nein/Enthaltung samt 100-%-Balken (`uebersicht/views.py:74-99`, `uebersicht.html:34-53`).
6. **Flash-Messages gehen bei htmx-Aktionen verloren**: Views antworten mit Redirect, htmx tauscht nur `#feld-…` → die Meldungen werden im unsichtbaren Vollrender konsumiert (Ausnahme: Favorit-Stern mit eigenem Partial).
7. **Prüfcode-Konzept unvollständig**: `StimmRegister.pruefcode` wird erzeugt, aber `eigene_stimme.html` zeigt nur das Pseudonym; Texte sprechen von „Prüfcode".
8. **Veraltete Verweise**: `einfuehrung.html:60-62` erklärt eine Suche „auf der Seite Lebensbereiche oben rechts" (`/kategorien/` ist nur noch Redirect); `nur_bestaetigte.html:10` und `antrag.html:146` verlinken hart `/willkommen/` bzw. `/umsetzung/`; `beitrag_verbuchen` (`mitglieder/models.py:332`) verlinkt hart `https://parlament.ddoe.at/beitrag/` statt `DDOE_BASIS_URL`; Parlament-Hinweis „im Profil hinterlegen" ohne existierende Profilseite.
9. **Ungenutztes**: Alpine.js 3.17.1 wird geladen, aber nie verwendet; CSS `.mini-kacheln`, `.brotkrume`, `blase-auf`, `.feld::after`; doppelte Regeln `:focus-visible` (`base.html:44` vs. `:299`), `.btn:hover` (`:109` vs. `:292`), zwei `prefers-reduced-motion`-Blöcke (`:306-309`, `:318`); Staffelung `auftauchen` endet beim 6. Kind, die Startseite hat 8 `.wk`-Karten.
10. **Verwaltung nur Deutsch**: 8 Templates ohne `i18n`, Nav „Verwaltung", Erinnerungs-/Bestätigungsmails, KI-Auftrag; `LANGUAGE_CODE="de-at"` vs. `LANGUAGES` „de".
11. **Mandatare**: `liste.html:53` gibt `{{ a.phase }}` roh aus (Slug statt `phase_name`); `Mandat.kandidatur` wird von keiner View gesetzt; Foto-Upload und Aufgabenstatus ohne Audit; `MandatFormular` ohne Kandidatur-Feld.
12. **Anstoß-Verwaltung**: `vermerk` wird aus POST gelesen, das Template bietet kein Eingabefeld (`anstoss/views.py:97`, `verwaltung.html:40-46`); Parameter-Verwaltung ist aus `/verwaltung/` nicht verlinkt.
13. **Drossel/Cache prozesslokal**: `botschutz.drossel_zuviel` nutzt den Default-LocMem-Cache — mit 2 Gunicorn-Workern nicht geteilt; Anstoß-Drossel rein sessionbasiert (neue Session = neues Kontingent).
14. **Unsichere Defaults**: `DDOE_DEBUG` Default „1", `SECRET_KEY` Default „nur-fuer-entwicklung…"; `demo_seed` läuft bei jedem Produktiv-Deploy (legt Demo-Mitglieder mit `@example.org` und Demo-Rollen an, sobald Tabellen leer sind).
15. **Performance/Skalierung**: `stimmberechtigte_zaehlen` iteriert alle Mitglieder in Python; `Gemeinde.finden` lädt alle 2092 Gemeinden je Aufruf (Registrierung/Verwaltung); `_kategorien_suchen` lädt alle 312 Kategorien; Registrierungsseite rendert 2092 `<option>`; `antrag_detail` schreibt (`fortschreiben`) bei jedem GET.
16. **Versionsangaben**: `pyproject.toml` `version = "0.1.0"`, `plattform_core.__version__ = "0.1.0"` vs. CHANGELOG 0.32.0; `Dockerfile`-CMD nutzt `runserver` (Render überschreibt).
17. **Koordinationsrat entscheidet einzeln** (kein Quorum), ebenso Gruppe 2 (eine Person validiert); nur Gruppe 1 hat eine dokumentierte Mehrheitsabstimmung.
18. **Unterstützungen und Kommentare ohne Audit-Eintrag** (im Gegensatz zu Stimmen, Bewerbungen, Verwaltung); Favoriten/Abos/Filter bewusst nicht auditiert.
19. **Naming**: Variable `antrag_der_g2` ist eine `Pruefung` (`gremien/views.py:502`); `Phase`-Werte enthalten „verfallen", die Übersicht zählt aber nur 5 Phasen (verfallen fehlt in „Anträge nach Phase", `uebersicht/views.py:118-124`).
20. **Dark Mode lückenhaft**: hart kodierte Badge-/Ikonen-Farben (siehe 3.3) und der QR-Kasten (`_beitrag_kasten.html:4`, `background:#fff`) bleiben hell; das Flussdiagramm auf `/` nutzt dagegen Tokens.
21. **Geburtsjahr wird abgefragt, aber nicht gespeichert**: `RegistrierungsFormular.geburtsjahr` dient nur der Altersprüfung; `Mitglied` hat kein entsprechendes Feld (`mitglieder/views.py:43,88-94,121-134`).
22. **Tests vs. Code-Zahlen**: 260 Testfunktionen ergeben 265 gesammelte Tests (Parametrisierung); der Coverage-Schwellwert 90 % gilt nur für `plattform_core` (`pyproject.toml:42,47`), nicht für die Django-Apps.
